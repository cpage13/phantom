{
  "components": {
    "schemas": {
      "AdminStatusResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/status`` aggregate shape (ADR-007).",
        "properties": {
          "ad_reachability": {
            "default": "not_configured",
            "description": "Set to reachable/unreachable when at least one instance configures ad_client_credentials.",
            "enum": [
              "reachable",
              "unreachable",
              "not_configured"
            ],
            "title": "Ad Reachability",
            "type": "string"
          },
          "disk_usage_bytes": {
            "description": "Sum of all instances' disk-tier byte usage.",
            "minimum": 0.0,
            "title": "Disk Usage Bytes",
            "type": "integer"
          },
          "implementation": {
            "default": "phantom-python",
            "description": "Which service implementation answered. The Python reference implementation reports phantom-python; a ported implementation reports its own identifier (e.g. phantom-go), so operators can always tell which binary served the request.",
            "title": "Implementation",
            "type": "string"
          },
          "instances": {
            "description": "Headline figures per instance.",
            "items": {
              "$ref": "#/components/schemas/InstanceSummary"
            },
            "title": "Instances",
            "type": "array"
          },
          "ready": {
            "description": "True when every instance's storage layer is open and accepting writes.",
            "title": "Ready",
            "type": "boolean"
          },
          "resolved_defaults": {
            "anyOf": [
              {
                "$ref": "#/components/schemas/ResolvedDefaultsSummary"
              },
              {
                "type": "null"
              }
            ],
            "description": "Resolved saturation/storage/worker defaults filled by the host probe at startup. Always populated when surfaced by the live admin route; ``None`` is permitted so unit tests can construct AdminStatusResponse without wiring a probe."
          },
          "service_version": {
            "default": "unknown",
            "description": "Version of the answering implementation (the phantom-service distribution version for the Python reference implementation).",
            "title": "Service Version",
            "type": "string"
          },
          "total_backlog": {
            "description": "Sum of in-flight rows across every instance.",
            "minimum": 0.0,
            "title": "Total Backlog",
            "type": "integer"
          }
        },
        "required": [
          "ready",
          "disk_usage_bytes",
          "total_backlog",
          "instances"
        ],
        "title": "AdminStatusResponse",
        "type": "object"
      },
      "AuthStatus": {
        "additionalProperties": false,
        "description": "Auth-cache summary for the stats endpoint.",
        "properties": {
          "auth_expired_count": {
            "description": "Number of rows currently parked in ``auth_expired``.",
            "minimum": 0.0,
            "title": "Auth Expired Count",
            "type": "integer"
          },
          "phantom_token_expires_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When configured ad_client_credentials' minted token expires; None when wait strategy is configured.",
            "title": "Phantom Token Expires At"
          }
        },
        "required": [
          "auth_expired_count"
        ],
        "title": "AuthStatus",
        "type": "object"
      },
      "BodyHashes": {
        "additionalProperties": false,
        "description": "Hash pair for one body_ref.\n\nbody_hash:    verifies codec round-trip and end-to-end byte\n              identity (computed pre-encode at ingress, compared\n              post-decode at send).\nstorage_hash: verifies storage integrity in RAM or on disk\n              (computed post-encode at ingress, compared pre-decode\n              at send).",
        "properties": {
          "body_hash": {
            "description": "SHA-256 hex of raw body bytes.",
            "title": "Body Hash",
            "type": "string"
          },
          "storage_hash": {
            "description": "SHA-256 hex of stored bytes.",
            "title": "Storage Hash",
            "type": "string"
          }
        },
        "required": [
          "body_hash",
          "storage_hash"
        ],
        "title": "BodyHashes",
        "type": "object"
      },
      "BulkDeleteResponse": {
        "additionalProperties": false,
        "description": "``DELETE /v1/admin/chains`` response shape.",
        "properties": {
          "deleted": {
            "description": "Number of rows deleted.",
            "minimum": 0.0,
            "title": "Deleted",
            "type": "integer"
          }
        },
        "required": [
          "deleted"
        ],
        "title": "BulkDeleteResponse",
        "type": "object"
      },
      "CapturedStep": {
        "additionalProperties": false,
        "description": "Captured values from one step, surfaced in ChainResponse on terminal-success.",
        "properties": {
          "step_name": {
            "description": "The producing step's name.",
            "title": "Step Name",
            "type": "string"
          },
          "values": {
            "additionalProperties": true,
            "description": "Captured values keyed by capture name; matches the step's declared captures.",
            "title": "Values",
            "type": "object"
          }
        },
        "required": [
          "step_name",
          "values"
        ],
        "title": "CapturedStep",
        "type": "object"
      },
      "CapturedStepValues": {
        "additionalProperties": false,
        "description": "One step's captured values plus TTL bookkeeping.",
        "properties": {
          "captured_at": {
            "description": "When this step produced its captures (UTC).",
            "format": "date-time",
            "title": "Captured At",
            "type": "string"
          },
          "expires_at": {
            "additionalProperties": {
              "anyOf": [
                {
                  "format": "date-time",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ]
            },
            "description": "Per-capture expiry timestamp. None when the capture is non-expiring (ChainCapture.ttl_seconds was unset).",
            "title": "Expires At",
            "type": "object"
          },
          "values": {
            "additionalProperties": true,
            "description": "Captured values keyed by capture name; JSON-shaped.",
            "title": "Values",
            "type": "object"
          }
        },
        "required": [
          "values",
          "captured_at",
          "expires_at"
        ],
        "title": "CapturedStepValues",
        "type": "object"
      },
      "CapturedValues": {
        "additionalProperties": false,
        "description": "Per-step captured-values store on the upload row.",
        "properties": {
          "steps": {
            "additionalProperties": {
              "$ref": "#/components/schemas/CapturedStepValues"
            },
            "description": "Keyed by ChainStep.name; one entry per completed step.",
            "title": "Steps",
            "type": "object"
          }
        },
        "title": "CapturedValues",
        "type": "object"
      },
      "ChainAdminDetail": {
        "additionalProperties": false,
        "description": "Admin-only chain detail \u2014 extends ChainResponse with inspection fields.\n\nReturned only by ``GET /v1/admin/chains/{chain_id}``. Loopback per\nADR-004. The wire-facing :class:`ChainResponse` stays unchanged;\nthis additional model exists so admin tools (and E2E test helpers)\ncan inspect ``body_location`` state without changing the SDK\ncontract or perturbing the byte-for-byte chain-model drift\ndetection in :file:`tests/contract/test_chain_models_alignment.py`.\n\nRound-2 extension (defender): the persisted request envelope is now\nsurfaced via :attr:`metadata` and :attr:`steps`. The user's explicit\nrequirement is \"if it is ever persisted, all that metadata is also\nretrievable with the files and the body\". The metadata KVS comes\nfrom the create-file step's JSON body (``step.body.value.metadata.\nkey_value_store``); the per-step request envelope is surfaced\nstructurally rather than via raw JSON so admin tooling can render\nit without re-parsing the chain envelope.\n\nThe pre-Phase-1\n``tier`` + ``committed`` fields collapsed into the single\n``body_location`` discriminator. ``ram`` corresponds to bodies the\nRAM body store holds; ``file`` to bodies the file body store holds\n(PersistController flipped the column after fsync). The\n``committed`` durability marker is gone \u2014 atomic admission +\nfsync-before-flip ordering make every persisted body durable by\nconstruction.\n\nCycle-7 task 4.5 extension: ``received_at``, ``updated_at``,\n``next_attempt_at``, ``sent_at``, ``group_id``, ``multifile_id``,\nand ``send_order`` are read straight off the upload row so the\nper-chain detail answers the delivery-time and grouping questions\nwithout a second lookup.",
        "properties": {
          "attempts": {
            "description": "Attempt count on the current step.",
            "minimum": 0.0,
            "title": "Attempts",
            "type": "integer"
          },
          "body_location": {
            "description": "Which body store currently holds the chain's body bytes. 'ram' = the RAM body store; 'file' = the file body store (PersistController flipped after fsync per plan \u00a7 0.5 invariant #6). Replaces the pre-Phase-1 tier + committed pair.",
            "enum": [
              "ram",
              "file"
            ],
            "title": "Body Location",
            "type": "string"
          },
          "captured": {
            "description": "Captured values from completed steps.",
            "items": {
              "$ref": "#/components/schemas/CapturedStep"
            },
            "title": "Captured",
            "type": "array"
          },
          "chain_id": {
            "description": "The chain's id.",
            "format": "uuid",
            "title": "Chain Id",
            "type": "string"
          },
          "group_id": {
            "description": "Query-grouping handle (the X-Phantom-Group-Id header value at admission, else chain_id; never null).",
            "format": "uuid",
            "title": "Group Id",
            "type": "string"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Short error string from most recent failed attempt; None if never failed.",
            "title": "Last Error"
          },
          "last_step_completed": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Name of last terminal-success step; None if none yet.",
            "title": "Last Step Completed"
          },
          "metadata": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Top-level metadata key-value store extracted from the chain envelope's create-file step (``step.body.value.metadata.key_value_store``). Empty dict when no step carries an inline JSON body with a ``metadata.key_value_store`` shape (e.g., purely body-ref chains). Surfaced byte-equal to what the producer submitted.",
            "title": "Metadata",
            "type": "object"
          },
          "multifile_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The multi-file set this upload belongs to; None when standalone. Distinct from the query group_id.",
            "title": "Multifile Id"
          },
          "next_attempt_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the sender will attempt this chain next (UTC); None when terminal or not scheduled.",
            "title": "Next Attempt At"
          },
          "received_at": {
            "description": "Ingress timestamp (UTC).",
            "format": "date-time",
            "title": "Received At",
            "type": "string"
          },
          "send_order": {
            "default": 0,
            "description": "Recorded position within the multi-file set; 0 for standalone uploads. Display only, never enforced.",
            "minimum": 0.0,
            "title": "Send Order",
            "type": "integer"
          },
          "sent_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "UTC instant the upload was confirmed delivered upstream; None until delivered. Stamped once, never moved.",
            "title": "Sent At"
          },
          "state": {
            "description": "Current chain state.",
            "enum": [
              "queued",
              "attempting",
              "succeeded",
              "failed",
              "auth_expired",
              "stored",
              "cancelled",
              "corrupted",
              "expired"
            ],
            "title": "State",
            "type": "string"
          },
          "steps": {
            "description": "Per-step request envelope projection. Each entry exposes method, url, headers, and has_body for the step as submitted. Empty list only when the envelope failed to round-trip (which would itself be a defect).",
            "items": {
              "$ref": "#/components/schemas/ChainAdminStepDetail"
            },
            "title": "Steps",
            "type": "array"
          },
          "updated_at": {
            "description": "When the row last transitioned (UTC).",
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          }
        },
        "required": [
          "chain_id",
          "state",
          "received_at",
          "updated_at",
          "group_id",
          "body_location",
          "attempts"
        ],
        "title": "ChainAdminDetail",
        "type": "object"
      },
      "ChainAdminStepDetail": {
        "additionalProperties": false,
        "description": "One step's request-envelope projection for the admin GET surface.\n\nThe persisted ChainStep carries method/url/headers/body plus\ncapture directives; this projection drops capture directives and\ncollapses the body to a single ``has_body`` flag (the body bytes\nare retrievable separately via ``GET /v1/admin/chains/{chain_id}/body``\nwhen the step's body is a ``body_ref`` and via the\n``chain_envelope_json`` round-trip when inline). The goal is to\nanswer the operator question \"what did the producer send?\" without\nre-shipping payload bytes through the metadata response.",
        "properties": {
          "has_body": {
            "description": "True iff this step declared a body at submission (inline JSON/text/bytes OR a body_ref pointing at uploaded bytes). The body payload itself is fetched separately via the body or bundle endpoints.",
            "title": "Has Body",
            "type": "boolean"
          },
          "headers": {
            "additionalProperties": {
              "type": "string"
            },
            "description": "Outbound headers configured on this step at submission. Surfaced byte-equal to what was submitted; values may still contain unresolved {{step.var}} placeholders.",
            "title": "Headers",
            "type": "object"
          },
          "method": {
            "description": "HTTP method this step issues to upstream.",
            "enum": [
              "GET",
              "POST",
              "PUT",
              "PATCH",
              "DELETE"
            ],
            "title": "Method",
            "type": "string"
          },
          "name": {
            "description": "Step name (envelope-defined, snake_case).",
            "title": "Name",
            "type": "string"
          },
          "url": {
            "description": "Target URL or path; may contain {{step.var}} placeholders that Phantom resolves at execution time. Surfaced byte-equal to what was submitted.",
            "title": "Url",
            "type": "string"
          }
        },
        "required": [
          "name",
          "method",
          "url",
          "has_body"
        ],
        "title": "ChainAdminStepDetail",
        "type": "object"
      },
      "CounterValue": {
        "additionalProperties": false,
        "description": "One counter entry: name, description, and label_value \u2192 count map.",
        "properties": {
          "description": {
            "description": "One-line human-readable description.",
            "title": "Description",
            "type": "string"
          },
          "name": {
            "description": "Canonical metric name (e.g., invariant_violation_total).",
            "title": "Name",
            "type": "string"
          },
          "values": {
            "additionalProperties": {
              "type": "integer"
            },
            "description": "Label-value bucket \u2192 count. The empty-string bucket ('') holds the no-label total for unlabeled counters.",
            "title": "Values",
            "type": "object"
          }
        },
        "required": [
          "name",
          "description",
          "values"
        ],
        "title": "CounterValue",
        "type": "object"
      },
      "CountersResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/observability/counters`` shape.",
        "properties": {
          "counters": {
            "description": "Every registered counter, with its current per-label counts.",
            "items": {
              "$ref": "#/components/schemas/CounterValue"
            },
            "title": "Counters",
            "type": "array"
          }
        },
        "required": [
          "counters"
        ],
        "title": "CountersResponse",
        "type": "object"
      },
      "DeleteFilter": {
        "additionalProperties": false,
        "description": "``DELETE /v1/admin/chains`` request body. Rejects empty filter (ADR-004).\n\n``since`` opts out of strict mode (``strict=False``) for the same\nwire reason as :class:`ExtractFilter.since`: the FastAPI dict path\nwill not coerce a JSON string to a ``datetime`` under strict mode, so\nthe documented since-window delete is otherwise un-submittable over\nHTTP. A malformed timestamp still raises a 422 (R-EX1).",
        "properties": {
          "instance": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to a single instance's rows.",
            "title": "Instance"
          },
          "route": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows whose resolved route matches.",
            "title": "Route"
          },
          "since": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows received at or after this UTC timestamp.",
            "title": "Since"
          },
          "state": {
            "anyOf": [
              {
                "enum": [
                  "queued",
                  "attempting",
                  "succeeded",
                  "failed",
                  "auth_expired",
                  "stored",
                  "cancelled",
                  "corrupted",
                  "expired"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows in this state.",
            "title": "State"
          }
        },
        "title": "DeleteFilter",
        "type": "object"
      },
      "ExtractFilter": {
        "additionalProperties": false,
        "description": "``POST /v1/admin/chains/extract`` request body.\n\nThe model is ``strict=True`` overall so typos (``extra=\"forbid\"``) and\ntype confusion on the string axes are rejected, but ``since`` and the\n``chain_ids`` elements opt out of strict mode (``strict=False``). The\nreason is the WIRE path: FastAPI's ``Body()`` parses the request JSON\nto a ``dict`` and validates it via ``model_validate`` (the dict path),\nand under strict mode the dict path refuses to coerce a JSON string to\na ``datetime`` / ``UUID`` (it demands native instances that JSON cannot\ncarry). Relaxing only these two axes lets the documented filters be\nsubmitted as the natural ISO-string / uuid-string JSON while a\nMALFORMED string still raises a ``ValidationError`` -> 422 (R-EX1).",
        "properties": {
          "chain_ids": {
            "anyOf": [
              {
                "items": {
                  "format": "uuid",
                  "type": "string"
                },
                "type": "array"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to this explicit list of chain_ids.",
            "title": "Chain Ids"
          },
          "instance": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to a single instance's rows.",
            "title": "Instance"
          },
          "route": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows whose resolved route matches.",
            "title": "Route"
          },
          "since": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows received at or after this UTC timestamp.",
            "title": "Since"
          },
          "state": {
            "anyOf": [
              {
                "enum": [
                  "queued",
                  "attempting",
                  "succeeded",
                  "failed",
                  "auth_expired",
                  "stored",
                  "cancelled",
                  "corrupted",
                  "expired"
                ],
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Restrict to rows in this state.",
            "title": "State"
          }
        },
        "title": "ExtractFilter",
        "type": "object"
      },
      "GaugeValue": {
        "additionalProperties": false,
        "description": "One gauge entry: name, description, and label_value \u2192 value map.",
        "properties": {
          "description": {
            "description": "One-line human-readable description.",
            "title": "Description",
            "type": "string"
          },
          "name": {
            "description": "Canonical metric name (e.g., saturation_balance).",
            "title": "Name",
            "type": "string"
          },
          "values": {
            "additionalProperties": {
              "type": "number"
            },
            "description": "Label-value bucket \u2192 current value. The empty-string bucket is the no-label total.",
            "title": "Values",
            "type": "object"
          }
        },
        "required": [
          "name",
          "description",
          "values"
        ],
        "title": "GaugeValue",
        "type": "object"
      },
      "GaugesResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/observability/gauges`` shape.",
        "properties": {
          "gauges": {
            "description": "Every registered gauge, with its current per-label values.",
            "items": {
              "$ref": "#/components/schemas/GaugeValue"
            },
            "title": "Gauges",
            "type": "array"
          }
        },
        "required": [
          "gauges"
        ],
        "title": "GaugesResponse",
        "type": "object"
      },
      "GroupMember": {
        "additionalProperties": false,
        "description": "One upload that belongs to a query group.",
        "properties": {
          "attempts": {
            "description": "Attempt count on the member's current step.",
            "minimum": 0.0,
            "title": "Attempts",
            "type": "integer"
          },
          "chain_id": {
            "description": "The member upload's chain_id (primary key).",
            "format": "uuid",
            "title": "Chain Id",
            "type": "string"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Short error string from the member's most recent failed attempt; None if never failed.",
            "title": "Last Error"
          },
          "multifile_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The multi-file set the member belongs to; None when the member arrived standalone. Distinct from the query group_id.",
            "title": "Multifile Id"
          },
          "received_at": {
            "description": "Ingress timestamp (UTC).",
            "format": "date-time",
            "title": "Received At",
            "type": "string"
          },
          "send_order": {
            "default": 0,
            "description": "The member's recorded position within its multi-file set; 0 for standalone uploads.",
            "minimum": 0.0,
            "title": "Send Order",
            "type": "integer"
          },
          "sent_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "UTC instant the member was confirmed delivered upstream; None until delivered. Stamped once, never moved.",
            "title": "Sent At"
          },
          "state": {
            "description": "The member's current chain state.",
            "enum": [
              "queued",
              "attempting",
              "succeeded",
              "failed",
              "auth_expired",
              "stored",
              "cancelled",
              "corrupted",
              "expired"
            ],
            "title": "State",
            "type": "string"
          }
        },
        "required": [
          "chain_id",
          "state",
          "received_at",
          "attempts"
        ],
        "title": "GroupMember",
        "type": "object"
      },
      "GroupStatusResponse": {
        "additionalProperties": false,
        "description": "Synthesized rollup for one query group.",
        "properties": {
          "all_finished": {
            "description": "True iff no member is in state queued or attempting. auth_expired and corrupted count as finished for the client: neither progresses without intervention. A token push that revives an auth_expired member flips this back to false while it re-attempts.",
            "title": "All Finished",
            "type": "boolean"
          },
          "counts_by_state": {
            "additionalProperties": {
              "type": "integer"
            },
            "description": "Histogram of member states across the nine canonical ChainState values.",
            "propertyNames": {
              "enum": [
                "queued",
                "attempting",
                "succeeded",
                "failed",
                "auth_expired",
                "stored",
                "cancelled",
                "corrupted",
                "expired"
              ]
            },
            "title": "Counts By State",
            "type": "object"
          },
          "first_received_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Earliest member received_at (UTC); None when the group is empty.",
            "title": "First Received At"
          },
          "group_id": {
            "description": "The query group this rollup describes (the path parameter).",
            "format": "uuid",
            "title": "Group Id",
            "type": "string"
          },
          "last_sent_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Latest member sent_at (UTC) across members that have been delivered; None when no member has been delivered yet.",
            "title": "Last Sent At"
          },
          "members": {
            "description": "One entry per member upload.",
            "items": {
              "$ref": "#/components/schemas/GroupMember"
            },
            "title": "Members",
            "type": "array"
          },
          "total": {
            "description": "Number of member uploads in the group.",
            "minimum": 0.0,
            "title": "Total",
            "type": "integer"
          }
        },
        "required": [
          "group_id",
          "total",
          "counts_by_state",
          "all_finished"
        ],
        "title": "GroupStatusResponse",
        "type": "object"
      },
      "HTTPValidationError": {
        "properties": {
          "detail": {
            "items": {
              "$ref": "#/components/schemas/ValidationError"
            },
            "title": "Detail",
            "type": "array"
          }
        },
        "title": "HTTPValidationError",
        "type": "object"
      },
      "HealthResponse": {
        "additionalProperties": false,
        "description": "Liveness probe response.\n\n``status`` is the liveness signal (always ``\"ok\"`` once the process\nis up). ``storage`` is the optional \u00a7 4D.2 storage-health signal: it\nreports whether every instance has writable durable storage, so an\noperator watching the liveness probe also sees a degraded-boot fault.\nThis model is extras-tolerated by the contract gate (the SDK mirror\nuses ``extra=\"ignore\"``), so the ``storage`` / ``storage_detail``\nfields are service-side additions that do not require an SDK change.",
        "properties": {
          "status": {
            "const": "ok",
            "description": "Liveness signal.",
            "title": "Status",
            "type": "string"
          },
          "storage": {
            "default": "ok",
            "description": "Storage health (\u00a7 4D.2). 'ok' when every instance has writable durable storage; 'degraded' when one or more instances booted with an unwritable data_dir (no durable buffering for that instance).",
            "enum": [
              "ok",
              "degraded"
            ],
            "title": "Storage",
            "type": "string"
          },
          "storage_detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Human-readable storage fault naming the degraded instance(s) when 'storage' is 'degraded'; None when storage is 'ok'.",
            "title": "Storage Detail"
          },
          "version": {
            "description": "Phantom version string.",
            "title": "Version",
            "type": "string"
          }
        },
        "required": [
          "status",
          "version"
        ],
        "title": "HealthResponse",
        "type": "object"
      },
      "IdentifierLookupResponse": {
        "additionalProperties": false,
        "description": "Result of an either-identifier lookup.",
        "properties": {
          "found": {
            "description": "True iff at least one upload matched.",
            "title": "Found",
            "type": "boolean"
          },
          "kind": {
            "description": "Which identifier axis was queried.",
            "enum": [
              "captured_file_id",
              "local_uuid"
            ],
            "title": "Kind",
            "type": "string"
          },
          "matches": {
            "description": "Matching uploads; usually zero or one. A list because Phantom enforces no global uniqueness on either identifier.",
            "items": {
              "$ref": "#/components/schemas/UploadStatusSummary"
            },
            "title": "Matches",
            "type": "array"
          },
          "value": {
            "description": "The identifier value queried.",
            "title": "Value",
            "type": "string"
          }
        },
        "required": [
          "kind",
          "value",
          "found"
        ],
        "title": "IdentifierLookupResponse",
        "type": "object"
      },
      "InstanceListResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/instances`` shape - the per-instance summary list.\n\nEnvelope rather than a bare array, to match the prevailing admin-list\nconvention (``GET /chains`` -> ``ListUploadsResponse``, ``GET /tokens``\n-> ``TokenListResponse``) and the SDK ``list_instances`` model (R-EX4).\nWrapping in an object also leaves room to grow (e.g. a future cursor)\nwithout breaking the wire shape.",
        "properties": {
          "instances": {
            "description": "Headline figures per configured instance.",
            "items": {
              "$ref": "#/components/schemas/InstanceSummary"
            },
            "title": "Instances",
            "type": "array"
          }
        },
        "required": [
          "instances"
        ],
        "title": "InstanceListResponse",
        "type": "object"
      },
      "InstanceStatusResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/instances/{id}/status`` shape.",
        "properties": {
          "auth": {
            "$ref": "#/components/schemas/AuthStatus",
            "description": "Auth-cache summary for this instance."
          },
          "by_state": {
            "$ref": "#/components/schemas/StateBreakdown",
            "description": "Per-state count + bytes breakdown for this instance."
          },
          "degraded_durability": {
            "default": false,
            "description": "True when disk write failures detected; surfaced in the admin status.",
            "title": "Degraded Durability",
            "type": "boolean"
          },
          "disk_usage_bytes": {
            "description": "Current bytes consumed by this instance's disk-tier data directory.",
            "minimum": 0.0,
            "title": "Disk Usage Bytes",
            "type": "integer"
          },
          "id": {
            "description": "InstanceCfg.id.",
            "title": "Id",
            "type": "string"
          },
          "in_flight": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "In-flight count + bytes for this instance."
          },
          "ready": {
            "description": "True when the instance's storage layer is open and accepting writes.",
            "title": "Ready",
            "type": "boolean"
          }
        },
        "required": [
          "id",
          "ready",
          "in_flight",
          "by_state",
          "auth",
          "disk_usage_bytes"
        ],
        "title": "InstanceStatusResponse",
        "type": "object"
      },
      "InstanceSummary": {
        "additionalProperties": false,
        "description": "One instance's headline figures, included in the aggregate status.",
        "properties": {
          "host_prefixes": {
            "description": "The fnmatch host prefixes this instance accepts.",
            "items": {
              "type": "string"
            },
            "title": "Host Prefixes",
            "type": "array"
          },
          "id": {
            "description": "InstanceCfg.id.",
            "title": "Id",
            "type": "string"
          },
          "in_flight": {
            "description": "Current in-flight row count for this instance.",
            "minimum": 0.0,
            "title": "In Flight",
            "type": "integer"
          },
          "refresh_strategy": {
            "description": "Selected refresh strategy plugin (ADR-001).",
            "enum": [
              "wait",
              "ad_client_credentials"
            ],
            "title": "Refresh Strategy",
            "type": "string"
          }
        },
        "required": [
          "id",
          "host_prefixes",
          "refresh_strategy",
          "in_flight"
        ],
        "title": "InstanceSummary",
        "type": "object"
      },
      "ListUploadsResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/chains`` shape \u2014 operator-detail rows + cursor.",
        "properties": {
          "next_cursor": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Opaque cursor for pagination; null when no more pages.",
            "title": "Next Cursor"
          },
          "uploads": {
            "description": "The page of upload-row detail records.",
            "items": {
              "$ref": "#/components/schemas/UploadRow"
            },
            "title": "Uploads",
            "type": "array"
          }
        },
        "required": [
          "uploads"
        ],
        "title": "ListUploadsResponse",
        "type": "object"
      },
      "ProfileRefCredBody": {
        "additionalProperties": false,
        "description": "``PUT`` admin-push body for a profile / default-chain reference.",
        "properties": {
          "kind": {
            "const": "profile_ref",
            "default": "profile_ref",
            "description": "Union discriminator; always profile_ref on this arm.",
            "title": "Kind",
            "type": "string"
          },
          "profile": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Named AWS profile resolved at sign time; None means the default chain.",
            "title": "Profile"
          },
          "region": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "AWS region for the resolved profile; None defers to the profile or chain.",
            "title": "Region"
          },
          "service": {
            "$ref": "#/components/schemas/SigningService",
            "description": "AWS service this credential signs for."
          }
        },
        "required": [
          "service"
        ],
        "title": "ProfileRefCredBody",
        "type": "object"
      },
      "QuarantineEntry": {
        "additionalProperties": false,
        "description": "One BACKUP (or one anomaly) surfaced via ``GET /v1/admin/quarantine``.\n\nPlan \u00a7 5.2.5 + cycle-7 seam 2. Backed by\n:func:`phantom.storage.integrity.list_quarantines`, which reads backup\nMANIFESTS: one entry per backup (keyed by :attr:`backup_id`), never one\nper loose artifact. An on-disk artifact no manifest claims surfaces as\nan ANOMALY entry (:attr:`anomaly` true, :attr:`backup_id` null) and is\nnot restorable. Retention of quarantine artifacts is admin-initiated by\ndefault (operator removes when ready); no auto-aging in v1.",
        "properties": {
          "anomaly": {
            "description": "``true`` for an on-disk artifact no manifest claims: surfaced for the operator, never restorable. ``false`` for a real (manifested) backup.",
            "title": "Anomaly",
            "type": "boolean"
          },
          "backup_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The backup's uuid identity (minted at creation; the key for ``POST /v1/admin/quarantine/restore?backup_id=...``). ``null`` iff this entry is an anomaly (an unmanifested artifact has no identity and cannot be restored).",
            "title": "Backup Id"
          },
          "body_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Declared absolute path of the backup's body-tree artifact, or ``null`` for a body-less backup (token-cache isolate) or a db-only anomaly.",
            "title": "Body Path"
          },
          "bytes": {
            "description": "Total size in bytes of the artifacts present on disk (DB file size plus the body tree's recursive file-size sum).",
            "minimum": 0.0,
            "title": "Bytes",
            "type": "integer"
          },
          "db_path": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Declared absolute path of the backup's DB artifact, or ``null`` for a body-only anomaly.",
            "title": "Db Path"
          },
          "has_body": {
            "description": "Whether the body-tree artifact is present on disk right now.",
            "title": "Has Body",
            "type": "boolean"
          },
          "has_db": {
            "description": "Whether the DB artifact is present on disk right now (the restorability signal: a restore requires the DB half).",
            "title": "Has Db",
            "type": "boolean"
          },
          "iso_display": {
            "description": "The human-readable ``%Y%m%dT%H%M%SZ`` timestamp half of the artifact names. Display and sort material ONLY; the backup's identity is ``backup_id``.",
            "title": "Iso Display",
            "type": "string"
          },
          "reason": {
            "description": "Why the backup was taken: ``corrupted`` (the integrity gate quarantined a failed-check DB + its body tree, or the body-less token-cache isolate) or ``mode_switch`` (an unsafe all_ram-over-populated-disk switch backed the live DB + body tree up before booting). A ``mode_switch`` backup is the only kind restorable via ``POST /v1/admin/quarantine/restore``. For an anomaly the reason is recovered from the artifact name for display only.",
            "enum": [
              "corrupted",
              "mode_switch"
            ],
            "title": "Reason",
            "type": "string"
          }
        },
        "required": [
          "backup_id",
          "reason",
          "iso_display",
          "db_path",
          "body_path",
          "has_db",
          "has_body",
          "bytes",
          "anomaly"
        ],
        "title": "QuarantineEntry",
        "type": "object"
      },
      "QuarantineInventoryResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/quarantine`` shape (plan \u00a7 5.2.5 / cycle-7 seam 2).",
        "properties": {
          "quarantines": {
            "description": "One entry per BACKUP (plus flagged anomaly entries) under the targeted instance data root(s). Scoped to one instance when ``?instance=`` is given, else aggregated across all instances. Empty list on a clean deployment.",
            "items": {
              "$ref": "#/components/schemas/QuarantineEntry"
            },
            "title": "Quarantines",
            "type": "array"
          }
        },
        "required": [
          "quarantines"
        ],
        "title": "QuarantineInventoryResponse",
        "type": "object"
      },
      "QuarantineRestoreResponse": {
        "additionalProperties": false,
        "description": "``POST /v1/admin/quarantine/restore`` response (plan \u00a7 1.5).",
        "properties": {
          "detail": {
            "description": "Human-readable instruction for the operator (e.g. restart steps).",
            "title": "Detail",
            "type": "string"
          },
          "interim_backup_body": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Absolute path of the body tree backed up from the live tree before the restore, or ``null`` when no live body tree existed.",
            "title": "Interim Backup Body"
          },
          "interim_backup_db": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Absolute path of the DB backed up from the live tree before the restore, or ``null`` when no live DB existed to back up.",
            "title": "Interim Backup Db"
          },
          "restart_required": {
            "description": "Always ``true`` in this design: the route STAGES the restore on disk; the running store keeps its old descriptor and will not serve the restored data until the operator restarts in a disk-backed mode.",
            "title": "Restart Required",
            "type": "boolean"
          },
          "restored_body": {
            "description": "Absolute path of the live body-store root after restore.",
            "title": "Restored Body",
            "type": "string"
          },
          "restored_db": {
            "description": "Absolute path of the live DB after the backup was moved in.",
            "title": "Restored Db",
            "type": "string"
          }
        },
        "required": [
          "restored_db",
          "restored_body",
          "interim_backup_db",
          "interim_backup_body",
          "restart_required",
          "detail"
        ],
        "title": "QuarantineRestoreResponse",
        "type": "object"
      },
      "RamPressureStatusResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/observability/ram_pressure`` shape (plan \u00a7 4.2.5).",
        "properties": {
          "pending_migrations": {
            "description": "In-flight PersistController migrations (queue depth + active).",
            "minimum": 0.0,
            "title": "Pending Migrations",
            "type": "integer"
          },
          "persist_controller_queue_depth": {
            "description": "Current PersistController.enqueue queue size.",
            "minimum": 0.0,
            "title": "Persist Controller Queue Depth",
            "type": "integer"
          },
          "ram_body_store_bytes": {
            "description": "Current RamBodyStore.total_bytes() observation.",
            "minimum": 0.0,
            "title": "Ram Body Store Bytes",
            "type": "integer"
          },
          "ram_ceiling_bytes": {
            "description": "Configured BodyStoreCfg.ram_ceiling_bytes (the cap that triggers pressure).",
            "minimum": 0.0,
            "title": "Ram Ceiling Bytes",
            "type": "integer"
          }
        },
        "required": [
          "ram_body_store_bytes",
          "ram_ceiling_bytes",
          "pending_migrations",
          "persist_controller_queue_depth"
        ],
        "title": "RamPressureStatusResponse",
        "type": "object"
      },
      "ReadyResponse": {
        "additionalProperties": false,
        "description": "Readiness probe response.",
        "properties": {
          "detail": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Human-readable reason if not ready.",
            "title": "Detail"
          },
          "ready": {
            "description": "True when DB open and accepting uploads.",
            "title": "Ready",
            "type": "boolean"
          }
        },
        "required": [
          "ready"
        ],
        "title": "ReadyResponse",
        "type": "object"
      },
      "ResolvedDefaultsSummary": {
        "additionalProperties": false,
        "description": "Resolved-default values an operator can read back via admin.\n\nEchoes the saturation / storage / worker numbers the model\nvalidator picked from :func:`phantom.config.defaults.compute_defaults`\noutput. Lets ``curl /v1/admin/status`` answer \"what cap am I\nactually running under?\" without parsing logs.",
        "properties": {
          "large_body_threshold_bytes": {
            "description": "Resolved large-body class size boundary.",
            "minimum": 0.0,
            "title": "Large Body Threshold Bytes",
            "type": "integer"
          },
          "max_disk_bytes": {
            "description": "Resolved persisted-tier disk-usage ceiling.",
            "minimum": 0.0,
            "title": "Max Disk Bytes",
            "type": "integer"
          },
          "max_in_flight": {
            "description": "Resolved saturation row-count cap.",
            "minimum": 0.0,
            "title": "Max In Flight",
            "type": "integer"
          },
          "max_in_flight_bytes": {
            "description": "Resolved saturation byte cap.",
            "minimum": 0.0,
            "title": "Max In Flight Bytes",
            "type": "integer"
          },
          "max_large_in_flight": {
            "description": "Resolved max concurrent in-flight large bodies.",
            "minimum": 0.0,
            "title": "Max Large In Flight",
            "type": "integer"
          },
          "observed_cpu_count": {
            "description": "``os.cpu_count() or 1`` observed at probe time.",
            "minimum": 1.0,
            "title": "Observed Cpu Count",
            "type": "integer"
          },
          "observed_free_disk_bytes": {
            "description": "``shutil.disk_usage(data_dir).free`` observed at probe time.",
            "minimum": 0.0,
            "title": "Observed Free Disk Bytes",
            "type": "integer"
          },
          "observed_total_ram_bytes": {
            "description": "``psutil.virtual_memory().total`` observed at probe time.",
            "minimum": 0.0,
            "title": "Observed Total Ram Bytes",
            "type": "integer"
          },
          "persist_body_size_threshold_bytes": {
            "description": "Resolved size-aware persist threshold.",
            "minimum": 0.0,
            "title": "Persist Body Size Threshold Bytes",
            "type": "integer"
          },
          "ram_ceiling_bytes": {
            "description": "Resolved RAM-tier body-store ceiling (post-rename from the pre-Phase-1 ``in_memory_max_bytes`` field; same semantic \u2014 bytes cap on the RamBodyStore).",
            "minimum": 0.0,
            "title": "Ram Ceiling Bytes",
            "type": "integer"
          },
          "worker_count": {
            "description": "Resolved sender worker-pool size.",
            "minimum": 1.0,
            "title": "Worker Count",
            "type": "integer"
          }
        },
        "required": [
          "max_in_flight",
          "max_in_flight_bytes",
          "max_disk_bytes",
          "ram_ceiling_bytes",
          "large_body_threshold_bytes",
          "max_large_in_flight",
          "persist_body_size_threshold_bytes",
          "worker_count",
          "observed_total_ram_bytes",
          "observed_free_disk_bytes",
          "observed_cpu_count"
        ],
        "title": "ResolvedDefaultsSummary",
        "type": "object"
      },
      "SaturationStatus": {
        "additionalProperties": false,
        "description": "Current saturation cap state.",
        "properties": {
          "max_in_flight": {
            "description": "Current row-count cap.",
            "minimum": 0.0,
            "title": "Max In Flight",
            "type": "integer"
          },
          "max_in_flight_bytes": {
            "description": "Current in-flight byte cap.",
            "minimum": 0.0,
            "title": "Max In Flight Bytes",
            "type": "integer"
          },
          "saturated": {
            "description": "True when either cap is at the limit and new submissions would be refused.",
            "title": "Saturated",
            "type": "boolean"
          }
        },
        "required": [
          "max_in_flight",
          "max_in_flight_bytes",
          "saturated"
        ],
        "title": "SaturationStatus",
        "type": "object"
      },
      "SigV4StaticCredBody": {
        "additionalProperties": false,
        "description": "``PUT`` admin-push body for a static SigV4 key-pair.\n\nCOPY of :class:`phantom.models.admin.TokenPushRequest` shape: resolved\nliterals only (env-var names live only on the config route), never returned\nin any response (ADR-004).",
        "properties": {
          "access_key_id": {
            "description": "Resolved AWS access key id literal, never an env-var name.",
            "minLength": 1,
            "title": "Access Key Id",
            "type": "string"
          },
          "kind": {
            "const": "sigv4_static",
            "default": "sigv4_static",
            "description": "Union discriminator; always sigv4_static on this arm.",
            "title": "Kind",
            "type": "string"
          },
          "region": {
            "description": "AWS region the signature is scoped to; the scope sibling of service.",
            "minLength": 1,
            "title": "Region",
            "type": "string"
          },
          "secret_access_key": {
            "description": "Resolved secret; never returned in any response (ADR-004).",
            "minLength": 1,
            "title": "Secret Access Key",
            "type": "string"
          },
          "service": {
            "$ref": "#/components/schemas/SigningService",
            "description": "AWS service this credential signs for."
          },
          "session_token": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "STS session token for temporary credentials; None for long-lived keys.",
            "title": "Session Token"
          }
        },
        "required": [
          "access_key_id",
          "secret_access_key",
          "region",
          "service"
        ],
        "title": "SigV4StaticCredBody",
        "type": "object"
      },
      "SigningService": {
        "description": "Closed set of AWS services Phantom can SigV4-sign for.\n\nA ``StrEnum``, so each member IS a ``str``: it serializes to its wire value\n(``\"s3\"``) for free at the pydantic boundary and at the SQLite write\n(``asdict`` + ``json.dumps``). Today the only implemented service is S3;\nadding another is a new member + a ``_SERVICE_SIGNERS`` entry (in\n``chain.sigv4_signer``), not a redesign.\n\nThe literal ``\"s3\"`` is defined HERE exactly once (the single source of the\nwire string); all logic references the symbol :attr:`SigningService.S3` by\ndot notation.",
        "enum": [
          "s3"
        ],
        "title": "SigningService",
        "type": "string"
      },
      "StateBreakdown": {
        "additionalProperties": false,
        "description": "Per-state count+bytes breakdown for the stats endpoint.",
        "properties": {
          "attempting": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Rows currently being attempted by the sender."
          },
          "auth_expired": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Rows parked waiting for a fresh token in the (endpoint, uid) cache."
          },
          "failed_recent": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Recently-failed rows still inside their retention window."
          },
          "queued": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Rows in the ``queued`` state."
          },
          "stored": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Rows in ``stored`` (body recoverable, retry budget exhausted)."
          },
          "succeeded_recent": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Recently-succeeded rows still inside their retention window."
          }
        },
        "required": [
          "queued",
          "attempting",
          "auth_expired",
          "stored",
          "succeeded_recent",
          "failed_recent"
        ],
        "title": "StateBreakdown",
        "type": "object"
      },
      "StatsResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/stats`` shape.",
        "properties": {
          "auth": {
            "$ref": "#/components/schemas/AuthStatus",
            "description": "Auth-cache summary (token expiry, auth_expired count)."
          },
          "body_location": {
            "additionalProperties": {
              "$ref": "#/components/schemas/TierBreakdown"
            },
            "description": "Per-body-location (ram / file) count + bytes breakdown. Replaces the pre-Phase-1 'tier' dict per plan \u00a7 2.3.19.",
            "propertyNames": {
              "enum": [
                "ram",
                "file"
              ]
            },
            "title": "Body Location",
            "type": "object"
          },
          "by_state": {
            "$ref": "#/components/schemas/StateBreakdown",
            "description": "Per-state count + bytes breakdown."
          },
          "in_flight": {
            "$ref": "#/components/schemas/TierBreakdown",
            "description": "Aggregate count + bytes of in-flight rows (queued+attempting+auth_expired)."
          },
          "parked_total": {
            "description": "Count of parked rows: stored (retry budget exhausted, body recoverable) plus auth_expired (waiting for a token).",
            "minimum": 0.0,
            "title": "Parked Total",
            "type": "integer"
          },
          "saturation": {
            "$ref": "#/components/schemas/SaturationStatus"
          }
        },
        "required": [
          "in_flight",
          "by_state",
          "body_location",
          "saturation",
          "auth",
          "parked_total"
        ],
        "title": "StatsResponse",
        "type": "object"
      },
      "TierBreakdown": {
        "additionalProperties": false,
        "description": "A count + bytes pair for one storage tier or state.",
        "properties": {
          "bytes": {
            "description": "Summed body bytes in this slice.",
            "minimum": 0.0,
            "title": "Bytes",
            "type": "integer"
          },
          "count": {
            "description": "Number of rows in this slice.",
            "minimum": 0.0,
            "title": "Count",
            "type": "integer"
          }
        },
        "required": [
          "count",
          "bytes"
        ],
        "title": "TierBreakdown",
        "type": "object"
      },
      "TokenListResponse": {
        "additionalProperties": false,
        "description": "``GET /v1/admin/tokens`` shape.",
        "properties": {
          "tokens": {
            "description": "Slot views of every (endpoint, uid) token cache entry (no bearer values).",
            "items": {
              "$ref": "#/components/schemas/TokenSlot"
            },
            "title": "Tokens",
            "type": "array"
          }
        },
        "required": [
          "tokens"
        ],
        "title": "TokenListResponse",
        "type": "object"
      },
      "TokenPushRequest": {
        "additionalProperties": false,
        "description": "``PUT /v1/admin/tokens/...`` request body.",
        "properties": {
          "token": {
            "description": "Bearer token value to store; never returned in any response.",
            "minLength": 1,
            "title": "Token",
            "type": "string"
          }
        },
        "required": [
          "token"
        ],
        "title": "TokenPushRequest",
        "type": "object"
      },
      "TokenSlot": {
        "additionalProperties": false,
        "description": "Admin-list view of a token cache slot. NO bearer field (ADR-004).",
        "properties": {
          "endpoint": {
            "description": "Upstream hostname (e.g., upstream.example.com).",
            "title": "Endpoint",
            "type": "string"
          },
          "last_updated": {
            "description": "When this slot's bearer was last written.",
            "format": "date-time",
            "title": "Last Updated",
            "type": "string"
          },
          "status": {
            "description": "Current freshness state (per ADR-003).",
            "enum": [
              "fresh",
              "bad",
              "unknown"
            ],
            "title": "Status",
            "type": "string"
          },
          "uid": {
            "description": "The opaque caller-supplied identifier.",
            "title": "Uid",
            "type": "string"
          }
        },
        "required": [
          "endpoint",
          "uid",
          "last_updated",
          "status"
        ],
        "title": "TokenSlot",
        "type": "object"
      },
      "UploadRow": {
        "additionalProperties": false,
        "description": "The persistent upload row.\n\nSchema-mirrored in the single persistent ``uploads`` table\n(post-Phase-1). Each field maps to a SQLite column except\n``captured_values`` which is serialized to ``captured_values_json``.",
        "properties": {
          "attempts": {
            "default": 0,
            "description": "Count of attempts made on the current step.",
            "minimum": 0.0,
            "title": "Attempts",
            "type": "integer"
          },
          "body_discarded_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the reaper deleted the body (per retention windows); None if body still present.",
            "title": "Body Discarded At"
          },
          "body_hashes": {
            "additionalProperties": {
              "$ref": "#/components/schemas/BodyHashes"
            },
            "description": "Per-body_ref hash pair, keyed by body_ref name. body_hash verifies end-to-end byte identity; storage_hash verifies storage integrity. Both are SHA-256 hex strings.",
            "title": "Body Hashes",
            "type": "object"
          },
          "body_location": {
            "description": "Whether body files for this chain live in RamBodyStore ('ram') or FileBodyStore ('file'). Single durability commit point \u2014 flipped only by the PersistController. See strategy \u00a73 invariant #1 / plan \u00a7 0.5 invariant #6.",
            "enum": [
              "ram",
              "file"
            ],
            "title": "Body Location",
            "type": "string"
          },
          "body_size_bytes": {
            "default": 0,
            "description": "Sum of all body_ref sizes for this upload (saturation accounting).",
            "minimum": 0.0,
            "title": "Body Size Bytes",
            "type": "integer"
          },
          "capture_reexecution_active": {
            "description": "ADR-011 instance YAML knob, captured at ingress so mid-chain YAML changes don't surprise in-flight chains.",
            "title": "Capture Reexecution Active",
            "type": "boolean"
          },
          "captured_values": {
            "$ref": "#/components/schemas/CapturedValues",
            "description": "Captured-values store; serialized to/from captured_values_json column."
          },
          "chain_envelope_json": {
            "description": "The validated ChainEnvelope persisted as JSON; survives restart.",
            "title": "Chain Envelope Json",
            "type": "string"
          },
          "chain_id": {
            "description": "Upload identifier; the chain_id from the submitted envelope.",
            "format": "uuid",
            "title": "Chain Id",
            "type": "string"
          },
          "chain_id_at_ingress": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The producer-supplied X-Phantom-Idempotency-Key captured at admission. Identifies a chain for admission-side dedup; distinct from ``idempotency_key`` (which Phantom forwards to upstream). None when the producer did not send the header. Stored on the row so admission can find an existing chain by the same ingress key even if the disk store's ``idempotency_index`` entry was reaped.",
            "title": "Chain Id At Ingress"
          },
          "current_step_index": {
            "default": 0,
            "description": "Index of the next step to attempt.",
            "minimum": 0.0,
            "title": "Current Step Index",
            "type": "integer"
          },
          "endpoint": {
            "description": "Hostname of the current step's target \u2014 ADR-002 cache axis.",
            "title": "Endpoint",
            "type": "string"
          },
          "group_id": {
            "description": "Query-grouping handle. Required: admission supplies the X-Phantom-Group-Id header value when present, else chain_id (every upload is a group of one by default).",
            "format": "uuid",
            "title": "Group Id",
            "type": "string"
          },
          "idempotency_key": {
            "description": "Envelope.idempotency_key; reused on every attempt.",
            "title": "Idempotency Key",
            "type": "string"
          },
          "instance_id": {
            "description": "The InstanceCfg.id this upload belongs to (ADR-006).",
            "title": "Instance Id",
            "type": "string"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Short error string from most recent failed attempt; None if never failed.",
            "title": "Last Error"
          },
          "last_step_completed": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Name of last terminal-success step; None if none yet.",
            "title": "Last Step Completed"
          },
          "multifile_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Multi-file association id. NULL means standalone (the row is not part of a multi-file set).",
            "title": "Multifile Id"
          },
          "next_attempt_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the sender should next try this row.",
            "title": "Next Attempt At"
          },
          "received_at": {
            "description": "Ingress timestamp (UTC).",
            "format": "date-time",
            "title": "Received At",
            "type": "string"
          },
          "route_name": {
            "description": "Resolved RouteCfg.name for the chain's first step.",
            "title": "Route Name",
            "type": "string"
          },
          "send_order": {
            "default": 0,
            "description": "Recorded position within the multi-file set. Display only; never enforced at delivery.",
            "minimum": 0.0,
            "title": "Send Order",
            "type": "integer"
          },
          "sent_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "When the upload was confirmed delivered upstream (UTC). Stamped once on confirmed delivery, never moved; survives replay. None until delivery.",
            "title": "Sent At"
          },
          "state": {
            "description": "Current state in the upload state machine.",
            "enum": [
              "queued",
              "attempting",
              "succeeded",
              "failed",
              "auth_expired",
              "stored",
              "cancelled",
              "corrupted",
              "expired"
            ],
            "title": "State",
            "type": "string"
          },
          "storage_encoding": {
            "default": "original",
            "description": "Phantom's added storage compression \u2014 invisible to upstream (req \u00a75d).",
            "enum": [
              "original",
              "zstd",
              "gzip"
            ],
            "title": "Storage Encoding",
            "type": "string"
          },
          "uid": {
            "description": "The opaque X-Phantom-Uid received at ingress; the secondary axis of the (endpoint, uid) token-cache key (ADR-002).",
            "title": "Uid",
            "type": "string"
          },
          "updated_at": {
            "description": "Most recent transition timestamp (UTC).",
            "format": "date-time",
            "title": "Updated At",
            "type": "string"
          },
          "upstream_response_headers_json": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Most recent upstream response headers (JSON).",
            "title": "Upstream Response Headers Json"
          },
          "upstream_status_code": {
            "anyOf": [
              {
                "type": "integer"
              },
              {
                "type": "null"
              }
            ],
            "description": "Most recent upstream HTTP status.",
            "title": "Upstream Status Code"
          }
        },
        "required": [
          "chain_id",
          "instance_id",
          "group_id",
          "route_name",
          "state",
          "body_location",
          "received_at",
          "updated_at",
          "endpoint",
          "uid",
          "chain_envelope_json",
          "idempotency_key",
          "capture_reexecution_active"
        ],
        "title": "UploadRow",
        "type": "object"
      },
      "UploadStatusSummary": {
        "additionalProperties": false,
        "description": "The 'did my upload land' status projection for one upload.",
        "properties": {
          "attempts": {
            "description": "Attempt count on the current step.",
            "minimum": 0.0,
            "title": "Attempts",
            "type": "integer"
          },
          "captured_file_id": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The upstream-assigned file id pulled from the captured file_information; None when not yet captured. Surfaced so the caller sees the correlation.",
            "title": "Captured File Id"
          },
          "chain_id": {
            "description": "The upload's chain_id (primary key).",
            "format": "uuid",
            "title": "Chain Id",
            "type": "string"
          },
          "instance_id": {
            "description": "The instance (ADR-006) this upload belongs to.",
            "title": "Instance Id",
            "type": "string"
          },
          "last_error": {
            "anyOf": [
              {
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "Short error string from the most recent failed attempt; None if never failed.",
            "title": "Last Error"
          },
          "local_uuid": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The phantom_local_uuid carried on the upstream request's metadata key-value store.",
            "title": "Local Uuid"
          },
          "multifile_id": {
            "anyOf": [
              {
                "format": "uuid",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "The multi-file set this upload belongs to; None when standalone. Distinct from the query group_id.",
            "title": "Multifile Id"
          },
          "received_at": {
            "description": "Ingress timestamp (UTC).",
            "format": "date-time",
            "title": "Received At",
            "type": "string"
          },
          "send_order": {
            "default": 0,
            "description": "Recorded position within the multi-file set; 0 for standalone uploads. Display only, never enforced.",
            "minimum": 0.0,
            "title": "Send Order",
            "type": "integer"
          },
          "sent_at": {
            "anyOf": [
              {
                "format": "date-time",
                "type": "string"
              },
              {
                "type": "null"
              }
            ],
            "description": "UTC instant the upload was confirmed delivered upstream; None until delivered.",
            "title": "Sent At"
          },
          "state": {
            "description": "Current chain state.",
            "enum": [
              "queued",
              "attempting",
              "succeeded",
              "failed",
              "auth_expired",
              "stored",
              "cancelled",
              "corrupted",
              "expired"
            ],
            "title": "State",
            "type": "string"
          }
        },
        "required": [
          "chain_id",
          "state",
          "received_at",
          "attempts",
          "instance_id"
        ],
        "title": "UploadStatusSummary",
        "type": "object"
      },
      "ValidationError": {
        "properties": {
          "ctx": {
            "title": "Context",
            "type": "object"
          },
          "input": {
            "title": "Input"
          },
          "loc": {
            "items": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "integer"
                }
              ]
            },
            "title": "Location",
            "type": "array"
          },
          "msg": {
            "title": "Message",
            "type": "string"
          },
          "type": {
            "title": "Error Type",
            "type": "string"
          }
        },
        "required": [
          "loc",
          "msg",
          "type"
        ],
        "title": "ValidationError",
        "type": "object"
      }
    }
  },
  "info": {
    "title": "phantom",
    "version": "0.1.0"
  },
  "openapi": "3.1.0",
  "paths": {
    "/v1/admin/chains": {
      "delete": {
        "description": "Bulk delete by filter.\n\nAn all-None filter is refused with the 422\n``bulk_delete_filter_empty`` envelope (ADR-004: an empty filter\nwould mean \"delete every row\").\n\nRaises:\n    BulkDeleteFilterEmptyError: When no filter field is set (422\n        ``bulk_delete_filter_empty`` envelope).",
        "operationId": "bulk_delete_uploads_v1_admin_chains_delete",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/DeleteFilter"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/BulkDeleteResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Bulk Delete Uploads"
      },
      "get": {
        "description": "List chains with filters + cursor pagination.\n\nSingle-store pagination (plan \u00a7 2.3.19) \u2014\nthe pre-Phase-1 dual-store fan-out is gone with the schema\ncollapse. The cursor opaquely carries the underlying store's\ncontinuation token.\n\n``body_location`` query parameter scopes by which body store\ncurrently holds the chain's bytes (``ram`` vs ``file``).\n\nThe ``key_value_match`` branch is a one-shot lookup (not paginated)\nbecause the underlying store helper returns at most ``limit`` rows\nper source with no continuation token. Its wire format is parsed by\n:func:`_parse_key_value_match`: the established first-colon split\nfor plain keys, the quoted-key form for keys containing a colon\n(round 2 defender fix R2-3; see the query parameter description).\n\nThe ``multifile_id`` filter returns the set ordered by\n``send_order`` (recorded position); it is one-shot like\n``key_value_match`` (a multi-file set is producer-scale small), so\ncombining it with ``cursor`` is a 422 ``multifile_cursor_conflict``\nenvelope and ``next_cursor`` is always null on its responses.\n``group_id`` filters by the query-grouping handle and paginates\nlike every other filter.\n\nRaises:\n    MultifileCursorConflictError: When ``multifile_id`` is combined\n        with ``cursor`` (422 ``multifile_cursor_conflict``\n        envelope).\n    KeyValueMatchInvalidError: When ``key_value_match`` does not\n        parse (422 ``key_value_match_invalid`` envelope).",
        "operationId": "list_uploads_v1_admin_chains_get",
        "parameters": [
          {
            "in": "query",
            "name": "state",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "queued",
                    "attempting",
                    "succeeded",
                    "failed",
                    "auth_expired",
                    "stored",
                    "cancelled",
                    "corrupted",
                    "expired"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "State"
            }
          },
          {
            "in": "query",
            "name": "route",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Route"
            }
          },
          {
            "in": "query",
            "name": "multifile_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Multifile Id"
            }
          },
          {
            "in": "query",
            "name": "group_id",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "uuid",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Group Id"
            }
          },
          {
            "in": "query",
            "name": "since",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "format": "date-time",
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Since"
            }
          },
          {
            "in": "query",
            "name": "limit",
            "required": false,
            "schema": {
              "default": 100,
              "maximum": 1000,
              "minimum": 1,
              "title": "Limit",
              "type": "integer"
            }
          },
          {
            "in": "query",
            "name": "cursor",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Cursor"
            }
          },
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          },
          {
            "description": "Match rows whose metadata key-value store carries this pair, written 'key:value' and split at the FIRST colon (so values may contain colons). A key that itself contains a colon, or begins with a double quote, rides the quoted-key form '\"<key>\":<value>' with backslash escapes for '\"' and '\\' inside the key (the SDK encodes this automatically). Key and value must both be non-empty; anything unparseable refuses with the 422 key_value_match_invalid envelope.",
            "in": "query",
            "name": "key_value_match",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "description": "Match rows whose metadata key-value store carries this pair, written 'key:value' and split at the FIRST colon (so values may contain colons). A key that itself contains a colon, or begins with a double quote, rides the quoted-key form '\"<key>\":<value>' with backslash escapes for '\"' and '\\' inside the key (the SDK encodes this automatically). Key and value must both be non-empty; anything unparseable refuses with the 422 key_value_match_invalid envelope.",
              "title": "Key Value Match"
            }
          },
          {
            "in": "query",
            "name": "body_location",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "enum": [
                    "ram",
                    "file"
                  ],
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Body Location"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ListUploadsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Uploads"
      }
    },
    "/v1/admin/chains/extract": {
      "post": {
        "description": "Stream a tar of matching bodies plus a manifest.json.",
        "operationId": "extract_uploads_v1_admin_chains_extract_post",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/ExtractFilter"
              }
            }
          },
          "required": true
        },
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Extract Uploads"
      }
    },
    "/v1/admin/chains/{chain_id}": {
      "delete": {
        "description": "Hard delete one chain + its body.\n\nReleases the saturation gate when the deleted row still held a slot\n(R8-4), using the accounting captured atomically with the DELETE.",
        "operationId": "delete_upload_v1_admin_chains__chain_id__delete",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Delete Upload"
      },
      "get": {
        "description": "Return a :class:`ChainAdminDetail` projection of one chain.\n\nAdmin-only (loopback per ADR-004). Extends the wire-facing\n:class:`ChainResponse` with ``body_location`` so operators and E2E\ntests can inspect storage state without changing the SDK contract.\n\nRound-2 extension: the persisted request envelope is surfaced via\n:attr:`ChainAdminDetail.metadata` and :attr:`ChainAdminDetail.steps`\nso an operator can answer \"what did the producer actually send?\" without\nfetching the body. The envelope is deserialized from the row's\n``chain_envelope_json`` column; no schema change is required.",
        "operationId": "get_upload_v1_admin_chains__chain_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ChainAdminDetail"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Upload"
      }
    },
    "/v1/admin/chains/{chain_id}/body": {
      "get": {
        "description": "Stream the body bytes.",
        "operationId": "get_upload_body_v1_admin_chains__chain_id__body_get",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Upload Body"
      }
    },
    "/v1/admin/chains/{chain_id}/bundle": {
      "get": {
        "description": "Return metadata + body as a simple JSON envelope.\n\nA real multipart implementation would require an SDK; the simple\nJSON envelope is sufficient for admin tooling and the v1 milestone.",
        "operationId": "get_upload_bundle_v1_admin_chains__chain_id__bundle_get",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Upload Bundle"
      }
    },
    "/v1/admin/chains/{chain_id}/cancel": {
      "post": {
        "description": "Transition the row to ``cancelled`` if non-terminal.\n\nThe cancel path OWNS the gate release for the row it cancels\n(R8-4): the sender's M-W4-F7 no-op deliberately skips release when\nits in-flight UPDATE finds the state changed, deferring to whoever\nchanged it. The release decision uses the store's in-transaction\n``previous_state`` (not the route's pre-fetch, which can race a\nsender/kicker transition) through the one shared\n:func:`row_holds_slot` predicate.",
        "operationId": "cancel_upload_v1_admin_chains__chain_id__cancel_post",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UploadRow"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Cancel Upload"
      }
    },
    "/v1/admin/chains/{chain_id}/replay": {
      "post": {
        "description": "Reset attempts and re-queue.\n\nM-W4-F7 audit closure: ``replay`` refuses when the row is currently\n``attempting`` (a sender is actively driving it). The store raises\n:class:`ReplayRefusedAttemptingError` from its in-write-lock state\nprecheck; the app-registered\n:func:`replay_refused_attempting_exception_handler` converts it\ninto the canonical 409 ``replay_refused_attempting`` envelope so\nthe operator can wait for the sender to finish (or cancel first).\nRound 1 defender fix (R1-1): pre-fix this refusal escaped as\nFastAPI's raw ``{\"detail\": ...}`` body, which the SDK could not\ndispatch on.\n\nBody-accounting refusal (cycle-7 phase 7 pre-round defender fix):\nthe store raises :class:`ReplayBodyDiscardedError` when the row's\n``body_discarded_at`` is stamped (the body is gone per the row's\nown accounting, on either discard leg). The app-registered\n:func:`replay_body_discarded_exception_handler` converts it into\nthe canonical 409 ``replay_body_discarded`` envelope; the row is\nleft exactly as it was (``sent_at`` preserved). Without the\nrefusal, the re-queued row would land in ``corrupted`` on the\nsender's next claim, laundering an operator action into a\ncorruption signal.\n\nRaises:\n    NotFoundError: When no instance holds the chain, including a\n        row deleted between the lookup and the store's write lock\n        (404 envelope).\n    ReplayBodyDiscardedError: When the row's body was already\n        discarded (409 ``replay_body_discarded`` envelope).\n    ReplayRefusedAttemptingError: When a sender is actively\n        driving the row (409 ``replay_refused_attempting``\n        envelope).",
        "operationId": "replay_upload_v1_admin_chains__chain_id__replay_post",
        "parameters": [
          {
            "in": "path",
            "name": "chain_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Chain Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UploadRow"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Replay Upload"
      }
    },
    "/v1/admin/credentials/{dest_host}": {
      "put": {
        "description": "Provision a destination credential for ``dest_host`` (loopback, no auth).\n\nThe SigV4 analogue of :func:`push_token_one` (the admin token push), per the\n2026-06-23 copy directive. Differs only by the route prefix\n(``/credentials`` vs ``/tokens``), the key (the destination host alone \u2014 a\nSigV4 step has no caller-supplied ``uid``), and the structured\n:data:`CredentialPushBody` (vs the bare token string).\n\nThe ``{dest_host}`` segment is normalized through the SAME ``_hostname``\nhelper the executor uses for its forward-time credential lookup\n(``chain/executor.py``), so the push key equals the lookup key\n``HostCredKey(_hostname(full_url))`` BY CONSTRUCTION \u2014 a host pushed as\n``S3.amazonaws.com`` resolves a request to ``s3.amazonaws.com`` (the\nsilent-miss class the token push left latent is closed here).\n\nEach instance's :attr:`~phantom.instances.context.InstanceContext.signer_creds`\nstore is ``set`` under that host key. ``set`` freshens the slot\n(``status='fresh'``) AND fires the store's wake handler, which the\n:class:`~phantom.workers.credential_kicker.CredentialKicker` registered \u2014 so\nan operator re-pushing fresh credentials for a host wakes every parked\n``auth_expired`` row on that host (the loop-closing seam, mirroring the\ntoken push waking the :class:`~phantom.workers.auth_kicker.AuthKicker`).\n\nThe :data:`CredentialPushBody` now declares a REQUIRED ``service`` (the AWS\nservice the credential signs for, coerced to a\n:class:`~phantom.models.credential.SigningService` at the strict boundary),\nso an operator PUT whose body omits ``service`` or names an unknown service\nis rejected ``422`` at the door (before any store write) by the body\nvalidator \u2014 the same fail-loud the config route applies at boot.\n\nA bearer-only deployment carries no credential store\n(``signer_creds is None``); such instances are skipped (the push is a no-op\nfor them \u2014 the store is optional by construction, ADR design), never a\ncrash.\n\nReturns ``204 No Content`` with NO body \u2014 the ``secret_access_key`` is\ntherefore never echoed (ADR-004). The admin surface exposes credential\nSTATUS only, never secret material.\n\nArgs:\n    dest_host: The destination host path segment (normalized at the door).\n    body: The discriminated credential-push wire body (resolved literals).\n    dispatcher: The instance dispatcher (the same DI seam the token push\n        uses, overridden at composition root).\n\nReturns:\n    An empty ``204`` response.",
        "operationId": "push_credential_one_v1_admin_credentials__dest_host__put",
        "parameters": [
          {
            "in": "path",
            "name": "dest_host",
            "required": true,
            "schema": {
              "title": "Dest Host",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "anyOf": [
                  {
                    "$ref": "#/components/schemas/SigV4StaticCredBody"
                  },
                  {
                    "$ref": "#/components/schemas/ProfileRefCredBody"
                  }
                ],
                "title": "Body"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Push Credential One"
      }
    },
    "/v1/admin/export.tar": {
      "get": {
        "description": "Stream every buffered body + manifest.json (ADR-005).",
        "operationId": "export_tar_v1_admin_export_tar_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Export Tar"
      }
    },
    "/v1/admin/groups/{group_id}": {
      "get": {
        "description": "Synthesized rollup for one query group (cycle-7 task 4.4).\n\nFans out over instances via the aggregate idiom (a group can\nlegitimately straddle instances; routing is per target URL prefix,\nADR-006) and folds the members in Python. 404 ONLY when no row\nanywhere carries the id; because ``group_id`` is NOT NULL and\ndefaults to ``chain_id`` at admission, a chain_id queried as a\ngroup id resolves to its self-evident singleton group (the accepted\nshared-value-space wart: ``total=1`` and the lone member's chain_id\nequals the queried id).\n\n``counts_by_state`` carries ALL NINE canonical states (zero counts\nincluded) so the histogram has a deterministic shape;\n``all_finished`` is the structural finished rule (see\n``_STILL_MOVING_STATES``); ``first_received_at`` / ``last_sent_at``\nare the min/max joins over the members. ``?instance=`` narrows the\nrollup to one instance (404 when the group has no rows there).",
        "operationId": "get_group_status_v1_admin_groups__group_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "group_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Group Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GroupStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Group Status"
      }
    },
    "/v1/admin/instances": {
      "get": {
        "description": "List all configured instances.\n\nReturns an ``{\"instances\": [...]}`` envelope (not a bare array) to\nmatch the SDK ``list_instances`` model and the prevailing admin-list\nconvention shared by ``/chains`` and ``/tokens`` (R-EX4).",
        "operationId": "list_instances_v1_admin_instances_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceListResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "List Instances"
      }
    },
    "/v1/admin/instances/{instance_id}/status": {
      "get": {
        "description": "Per-instance admin status.\n\nRaises 421 ``instance_unknown`` when the named instance is not\nconfigured (plan \u00a75.6 error table); the app-level handler converts\nthat into a canonical ``ErrorEnvelope`` response.",
        "operationId": "get_instance_status_v1_admin_instances__instance_id__status_get",
        "parameters": [
          {
            "in": "path",
            "name": "instance_id",
            "required": true,
            "schema": {
              "title": "Instance Id",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/InstanceStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Instance Status"
      }
    },
    "/v1/admin/observability/counters": {
      "get": {
        "description": "Serialize the process-wide MetricsRegistry counters.\n\nPlan \u00a7 4.2.5. Returns one :class:`CounterValue` per registered\ncounter; ``values`` maps the empty-string bucket plus any label\nvalues that have been incremented. Even unbumped counters surface\na zero-valued bucket for stable response shape.",
        "operationId": "get_observability_counters_v1_admin_observability_counters_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/CountersResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Observability Counters"
      }
    },
    "/v1/admin/observability/gauges": {
      "get": {
        "description": "Serialize the process-wide MetricsRegistry gauges.\n\nPlan \u00a7 4.2.5. ``body_location_distribution`` is registered by the\nstore but populated on demand here via a SQL grouping across every\ninstance's persistent store (the gauge has no producer per plan\n\u00a7 4.2.2 to avoid invariant-coupled per-write drift).",
        "operationId": "get_observability_gauges_v1_admin_observability_gauges_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/GaugesResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Observability Gauges"
      }
    },
    "/v1/admin/observability/ram_pressure": {
      "get": {
        "description": "Surface RAM-pressure status across all instances.\n\nPlan \u00a7 4.2.5. Aggregates ``ram_body_store.total_bytes()`` +\n``persist_controller`` queue depth across every instance. Returns\nzero for fields whose instance configuration disables them (e.g.,\n``persist_controller_queue_depth = 0`` in all_ram / all_disk where\nno PersistController is wired).",
        "operationId": "get_observability_ram_pressure_v1_admin_observability_ram_pressure_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/RamPressureStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Observability Ram Pressure"
      }
    },
    "/v1/admin/quarantine": {
      "get": {
        "description": "List backups (one entry each, plus anomalies) for one or all instances.\n\nPlan \u00a7 5.2.5 / cycle-7 seam 2. Returns ONE :class:`QuarantineEntry` per\nBACKUP under the targeted instance data root(s), read from the backup\nmanifests (keyed by ``backup_id``), plus one flagged anomaly entry per\non-disk artifact no manifest claims. Retention is admin-initiated by\ndefault (no auto-aging).\n\nScope (Finding F-1):\n\n* ``?instance=<id>`` \u2014 scan just that instance's per-instance data_root.\n* omitted \u2014 scan every configured instance and concatenate the results\n  (the natural \"everything quarantined across the deployment\" view).\n\nRaises:\n    UnknownInstanceError: When ``?instance=`` names an unknown instance\n        (becomes a 421 ``ErrorEnvelope``).",
        "operationId": "get_quarantine_inventory_v1_admin_quarantine_get",
        "parameters": [
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuarantineInventoryResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Quarantine Inventory"
      }
    },
    "/v1/admin/quarantine/restore": {
      "post": {
        "description": "Move a ``mode_switch`` backup back into the live tree (plan \u00a7 1.5).\n\nStrategy E.1 + cycle-7 seam 2: an explicit, clobber-safe one-call admin\nrestore addressed by IDENTITY\n(``POST /v1/admin/quarantine/restore?backup_id=...&instance=...``). The\nbackup is resolved through its MANIFEST; the route never string-matches\nfilenames, so an unmanifested artifact (an inventory anomaly) is simply\nnot addressable here (R5-P unrepresentable). Backs up any CURRENT live\ndata to a fresh ``mode_switch`` backup first, then moves the chosen\nbackup into the now-empty live tree. Both moves are marked, so a crash\nat any point is finished forward by\n:func:`reconcile_interrupted_backup_move` on the next boot (review M-1).\n\nThis STAGES the restore on disk; it does NOT hot-reattach the running\nstore, which keeps its old file descriptor (Finding F-3). The operator\nmust restart in a disk-backed mode (``hybrid`` / ``all_disk``) to serve\nthe restored data; the response says so (``restart_required=True``).\n\nScope (Finding F-1): ``?instance=`` targets whose live tree to restore\nINTO. Required when more than one instance is configured; defaults to the\nsole instance otherwise. A restore is a single-target destructive\noperation, so there is no aggregate form.\n\nRaises:\n    UnknownInstanceError: When ``?instance=`` names an unknown instance\n        (becomes a 421 ``ErrorEnvelope``).\n    NotFoundError: When ``?instance=`` is omitted with more than one\n        instance configured, or when ``backup_id`` names no restorable\n        ``mode_switch`` backup manifest in the instance (becomes a 404\n        ``ErrorEnvelope``).\n    RestoreNoOpError: When the backup's DB half (the load-bearing half:\n        it holds the upload rows that point at the body bytes) is absent\n        on disk, the route refuses UP FRONT, before displacing any live\n        data; and when the restore moves ran but the DB still did not\n        land, it fails after the fact. Both become a 409 ``restore_noop``\n        ``ErrorEnvelope`` so the operator retries rather than receiving\n        a success-shaped response that stranded the upload metadata\n        (findings H-1 / L-2 / R5-P).",
        "operationId": "restore_quarantine_backup_v1_admin_quarantine_restore_post",
        "parameters": [
          {
            "in": "query",
            "name": "backup_id",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Backup Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/QuarantineRestoreResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Restore Quarantine Backup"
      }
    },
    "/v1/admin/reload": {
      "post": {
        "description": "Trigger a settings reload from the configured YAML path.\n\nMirrors SIGHUP: re-reads the YAML at ``app.state.settings_path``,\nbuilds fresh per-instance snapshots, swaps them in the\n:class:`SettingsHolder`, restarts AD-mint loops whose config\nchanged, and pushes new saturation caps into every gate.\n\nReturns:\n    200 ``{\"reloaded_instances\": [<id>, ...]}`` on success.\n    422 ``ErrorEnvelope`` (``envelope_invalid``) on YAML parse or\n    Pydantic validation failure.\n    404 ``ErrorEnvelope`` (``not_found``) when hot reload is\n    disabled because ``create_app`` was given no ``settings_path``.",
        "operationId": "post_reload_v1_admin_reload_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Post Reload"
      }
    },
    "/v1/admin/stats": {
      "get": {
        "description": "Aggregate counters across instances (optionally scoped).",
        "operationId": "get_stats_v1_admin_stats_get",
        "parameters": [
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/StatsResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Get Stats"
      }
    },
    "/v1/admin/status": {
      "get": {
        "description": "Aggregate admin status.",
        "operationId": "get_admin_status_v1_admin_status_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/AdminStatusResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Admin Status"
      }
    },
    "/v1/admin/tokens": {
      "delete": {
        "description": "Invalidate every slot across every instance: mark ``bad``, preserve.\n\nThe bulk analogue of :func:`delete_token_one`; per ADR-003 the slots\npersist as ``status='bad'`` rather than being deleted (R-EX3).",
        "operationId": "delete_tokens_all_v1_admin_tokens_delete",
        "responses": {
          "204": {
            "description": "Successful Response"
          }
        },
        "summary": "Delete Tokens All"
      },
      "get": {
        "description": "List token slots (NO bearer values; ADR-004).",
        "operationId": "list_tokens_v1_admin_tokens_get",
        "parameters": [
          {
            "in": "query",
            "name": "endpoint",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Endpoint"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/TokenListResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "List Tokens"
      },
      "put": {
        "description": "Push a bearer to every cached slot.",
        "operationId": "push_token_global_v1_admin_tokens_put",
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenPushRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Push Token Global"
      }
    },
    "/v1/admin/tokens/{endpoint}": {
      "put": {
        "description": "Push a bearer to every slot at an endpoint.",
        "operationId": "push_token_endpoint_v1_admin_tokens__endpoint__put",
        "parameters": [
          {
            "in": "path",
            "name": "endpoint",
            "required": true,
            "schema": {
              "title": "Endpoint",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenPushRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Push Token Endpoint"
      }
    },
    "/v1/admin/tokens/{endpoint}/{uid}": {
      "delete": {
        "description": "Invalidate one ``(endpoint, uid)`` slot: mark it ``bad``, preserve it.\n\nPer ADR-003 a bad token is NOT deleted - it stays in the cache with\n``status='bad'`` so ``GET /v1/admin/tokens`` still surfaces the slot\nand an operator can see exactly which credential needs replacement.\nThis route therefore flips the slot's status rather than hard-deleting\nit (R-EX3); the SDK ``invalidate_token`` contract matches.",
        "operationId": "delete_token_one_v1_admin_tokens__endpoint___uid__delete",
        "parameters": [
          {
            "in": "path",
            "name": "endpoint",
            "required": true,
            "schema": {
              "title": "Endpoint",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "uid",
            "required": true,
            "schema": {
              "title": "Uid",
              "type": "string"
            }
          }
        ],
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Delete Token One"
      },
      "put": {
        "description": "Push a bearer for one slot.",
        "operationId": "push_token_one_v1_admin_tokens__endpoint___uid__put",
        "parameters": [
          {
            "in": "path",
            "name": "endpoint",
            "required": true,
            "schema": {
              "title": "Endpoint",
              "type": "string"
            }
          },
          {
            "in": "path",
            "name": "uid",
            "required": true,
            "schema": {
              "title": "Uid",
              "type": "string"
            }
          }
        ],
        "requestBody": {
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/TokenPushRequest"
              }
            }
          },
          "required": true
        },
        "responses": {
          "204": {
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Push Token One"
      }
    },
    "/v1/admin/uploads/by-captured-id/{captured_id}": {
      "get": {
        "description": "Look up uploads by the upstream-assigned captured id (cycle-7 task 4.4).\n\nThe identifier lives inside ``captured_values_json``; WHERE it lives\nis bound by per-instance deployment configuration\n(``InstanceCfg.admin_lookup``), so the service stays\nupstream-ignorant and the route takes no query params beyond\n``?instance=``. Every targeted instance must carry the binding;\notherwise the lookup refuses with ``lookup_not_configured`` (HTTP\n400) rather than guessing or silently skipping an instance (a\nfound=false answer that ignored an unconfigured instance would be a\nlie). A miss is 200 with ``found=false``: the question is a\nmembership test, not a resource fetch.\n\nThe bindings are snapshotted ONCE, before the unconfigured guard,\nand the fan-out runs entirely off that snapshot: a config reload\nrepointing ``ctx.cfg`` mid-request cannot make the loop's per-ctx\nre-read skip the instance holding the match (the found=false lie\nthe round 2 adversary proved, finding R2-4). The answer is the\ntruth as of guard time; the reloaded bindings serve the next\nrequest.",
        "operationId": "lookup_by_captured_id_v1_admin_uploads_by_captured_id__captured_id__get",
        "parameters": [
          {
            "in": "path",
            "name": "captured_id",
            "required": true,
            "schema": {
              "title": "Captured Id",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IdentifierLookupResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Lookup By Captured Id"
      }
    },
    "/v1/admin/uploads/by-local-uuid/{local_uuid}": {
      "get": {
        "description": "Look up uploads by their producer-minted local uuid (cycle-7 task 4.4).\n\nFirst-class promotion of the generic\n``?key_value_match=phantom_local_uuid:<v>`` lookup: the metadata key\nis PINNED store-side (the caller never spells a key or a JSON path)\nand the result is the same status projection the captured-id lookup\nreturns. Needs NO per-instance configuration (the key is a fixed\nPhantom convention). A miss is 200 with ``found=false``; matches is\na list because Phantom enforces no global uniqueness on the key.\n\nThe per-instance ``admin_lookup`` bindings (used only for the\nbest-effort ``captured_file_id`` surfacing field here) are\nsnapshotted once before the fan-out, the same reload-race posture\nas the by-captured-id route (R2-4).",
        "operationId": "lookup_by_local_uuid_v1_admin_uploads_by_local_uuid__local_uuid__get",
        "parameters": [
          {
            "in": "path",
            "name": "local_uuid",
            "required": true,
            "schema": {
              "format": "uuid",
              "title": "Local Uuid",
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "instance",
            "required": false,
            "schema": {
              "anyOf": [
                {
                  "type": "string"
                },
                {
                  "type": "null"
                }
              ],
              "title": "Instance"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/IdentifierLookupResponse"
                }
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Lookup By Local Uuid"
      }
    },
    "/v1/healthz": {
      "get": {
        "description": "Liveness probe (the process is up).\n\n``status`` stays ``\"ok\"`` (liveness is about the process being alive,\nnot about storage health) so existing liveness checks are unaffected.\nThe optional ``storage`` field is the fuller signal (\u00a7 4D.2 / N-2):\n``\"ok\"`` when every instance has writable storage, else\n``\"degraded\"`` with ``storage_detail`` naming the degraded instance(s)\nand their faults. ``HealthResponse`` is extras-tolerated by the\ncontract gate, so the added service-side field does not break the SDK\nmirror.",
        "operationId": "get_health_v1_healthz_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HealthResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Health"
      }
    },
    "/v1/readyz": {
      "get": {
        "description": "Readiness probe: true once every instance's DB is open and writable.\n\nReturns ``ready=false`` when no instance is configured (the existing\nbehavior) OR when one or more instances booted DEGRADED (\u00a7 4D.2): an\nunwritable per-instance ``data_dir`` means that instance has no durable\nbuffering, so the deployment is not fully ready and the ``detail``\nnames the degraded instance(s) and their faults.",
        "operationId": "get_ready_v1_readyz_get",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/ReadyResponse"
                }
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Get Ready"
      }
    },
    "/v1/send": {
      "post": {
        "description": "Ingress endpoint \u2014 accept a chain submission and return 202.\n\nThe handler stays HTTP-shaped: parse headers (the grouping/ordering\ntrio through :func:`_parse_grouping_headers`), then\n:func:`_parse_and_resolve` (Content-Length precheck, body parsing,\nthe host-route \u00a7 4D.2 degraded guard, dispatcher resolution),\ndelegate to :func:`admit_chain`, build the response. Every refusal\nbecomes a :class:`ChainAdmissionError` raised here or by\n``admit_chain``; the handler maps those to the canonical\n:class:`ErrorEnvelope` response (see :func:`_degraded_guard_response`\nfor the degraded-boot guard rationale).",
        "operationId": "post_send_v1_send_post",
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          }
        },
        "summary": "Post Send"
      }
    },
    "/{phantom_path}": {
      "delete": {
        "description": "Preserve a bare 404 for read/metadata verbs on the catch-all.\n\nA root-mounted ``/{phantom_path:path}`` route bound to the upload verbs\nwould otherwise make every unknown ``GET`` / ``HEAD`` / ``DELETE`` /\n``OPTIONS`` return 405 (method-not-allowed) service-wide, because the\npath now matches. Registering this complementary-method arm (in the SAME\nrouter, AFTER :func:`raw_intake`) restores the prior behavior: an unknown\npath under a non-upload verb is 404, exactly as before the catch-all\nexisted. ``DELETE`` is out of scope by design and stays 404 here \u2014 it is\nNOT an object-delete surface.\n\nArgs:\n    phantom_path: The matched path (unused; the arm answers uniformly).\n\nReturns:\n    A bare 404 :class:`Response`.",
        "operationId": "phantom_path_delete",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake Unsupported"
      },
      "get": {
        "description": "Preserve a bare 404 for read/metadata verbs on the catch-all.\n\nA root-mounted ``/{phantom_path:path}`` route bound to the upload verbs\nwould otherwise make every unknown ``GET`` / ``HEAD`` / ``DELETE`` /\n``OPTIONS`` return 405 (method-not-allowed) service-wide, because the\npath now matches. Registering this complementary-method arm (in the SAME\nrouter, AFTER :func:`raw_intake`) restores the prior behavior: an unknown\npath under a non-upload verb is 404, exactly as before the catch-all\nexisted. ``DELETE`` is out of scope by design and stays 404 here \u2014 it is\nNOT an object-delete surface.\n\nArgs:\n    phantom_path: The matched path (unused; the arm answers uniformly).\n\nReturns:\n    A bare 404 :class:`Response`.",
        "operationId": "phantom_path_get",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake Unsupported"
      },
      "head": {
        "description": "Preserve a bare 404 for read/metadata verbs on the catch-all.\n\nA root-mounted ``/{phantom_path:path}`` route bound to the upload verbs\nwould otherwise make every unknown ``GET`` / ``HEAD`` / ``DELETE`` /\n``OPTIONS`` return 405 (method-not-allowed) service-wide, because the\npath now matches. Registering this complementary-method arm (in the SAME\nrouter, AFTER :func:`raw_intake`) restores the prior behavior: an unknown\npath under a non-upload verb is 404, exactly as before the catch-all\nexisted. ``DELETE`` is out of scope by design and stays 404 here \u2014 it is\nNOT an object-delete surface.\n\nArgs:\n    phantom_path: The matched path (unused; the arm answers uniformly).\n\nReturns:\n    A bare 404 :class:`Response`.",
        "operationId": "phantom_path_head",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake Unsupported"
      },
      "options": {
        "description": "Preserve a bare 404 for read/metadata verbs on the catch-all.\n\nA root-mounted ``/{phantom_path:path}`` route bound to the upload verbs\nwould otherwise make every unknown ``GET`` / ``HEAD`` / ``DELETE`` /\n``OPTIONS`` return 405 (method-not-allowed) service-wide, because the\npath now matches. Registering this complementary-method arm (in the SAME\nrouter, AFTER :func:`raw_intake`) restores the prior behavior: an unknown\npath under a non-upload verb is 404, exactly as before the catch-all\nexisted. ``DELETE`` is out of scope by design and stays 404 here \u2014 it is\nNOT an object-delete surface.\n\nArgs:\n    phantom_path: The matched path (unused; the arm answers uniformly).\n\nReturns:\n    A bare 404 :class:`Response`.",
        "operationId": "phantom_path_options",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake Unsupported"
      },
      "patch": {
        "description": "Accept a stock object-storage upload and buffer it as a chain.\n\nThe raw-intake landing (TASK 1.1/1.2/1.3). Reserved-prefix guard \u2192\ndestination resolution (421 when unroutable, BEFORE any durable write)\n\u2192 Content-Length precheck \u2192 body read (capped) \u2192 raw\u2192envelope synthesis\n\u2192 the shared :func:`resolve_and_admit` prelude (degraded guard, dispatch,\n:func:`admit_chain`). Returns the same 202 + ``X-Phantom-*`` response a\nproducer-supplied chain receives, or the canonical error envelope.\n\nArgs:\n    phantom_path: The object path after the host (e.g. ``bucket/key``).\n    request: The inbound raw-intake request.\n    dispatcher: The live instance dispatcher (DI).\n    max_buffered_bytes: The per-upload byte cap (DI).\n    instance_cfgs: Configured instances, for the degraded guard (DI).\n    degraded_instances: The typed degraded set, for the guard (DI).\n    phantom_default_target: The configured default upstream target (DI).\n\nReturns:\n    A 202 :class:`Response` on admission, or a canonical error\n    :class:`Response` (404 reserved prefix, 421 no destination, 413\n    oversized, or any admission refusal).",
        "operationId": "phantom_path_patch",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake"
      },
      "post": {
        "description": "Accept a stock object-storage upload and buffer it as a chain.\n\nThe raw-intake landing (TASK 1.1/1.2/1.3). Reserved-prefix guard \u2192\ndestination resolution (421 when unroutable, BEFORE any durable write)\n\u2192 Content-Length precheck \u2192 body read (capped) \u2192 raw\u2192envelope synthesis\n\u2192 the shared :func:`resolve_and_admit` prelude (degraded guard, dispatch,\n:func:`admit_chain`). Returns the same 202 + ``X-Phantom-*`` response a\nproducer-supplied chain receives, or the canonical error envelope.\n\nArgs:\n    phantom_path: The object path after the host (e.g. ``bucket/key``).\n    request: The inbound raw-intake request.\n    dispatcher: The live instance dispatcher (DI).\n    max_buffered_bytes: The per-upload byte cap (DI).\n    instance_cfgs: Configured instances, for the degraded guard (DI).\n    degraded_instances: The typed degraded set, for the guard (DI).\n    phantom_default_target: The configured default upstream target (DI).\n\nReturns:\n    A 202 :class:`Response` on admission, or a canonical error\n    :class:`Response` (404 reserved prefix, 421 no destination, 413\n    oversized, or any admission refusal).",
        "operationId": "phantom_path_post",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake"
      },
      "put": {
        "description": "Accept a stock object-storage upload and buffer it as a chain.\n\nThe raw-intake landing (TASK 1.1/1.2/1.3). Reserved-prefix guard \u2192\ndestination resolution (421 when unroutable, BEFORE any durable write)\n\u2192 Content-Length precheck \u2192 body read (capped) \u2192 raw\u2192envelope synthesis\n\u2192 the shared :func:`resolve_and_admit` prelude (degraded guard, dispatch,\n:func:`admit_chain`). Returns the same 202 + ``X-Phantom-*`` response a\nproducer-supplied chain receives, or the canonical error envelope.\n\nArgs:\n    phantom_path: The object path after the host (e.g. ``bucket/key``).\n    request: The inbound raw-intake request.\n    dispatcher: The live instance dispatcher (DI).\n    max_buffered_bytes: The per-upload byte cap (DI).\n    instance_cfgs: Configured instances, for the degraded guard (DI).\n    degraded_instances: The typed degraded set, for the guard (DI).\n    phantom_default_target: The configured default upstream target (DI).\n\nReturns:\n    A 202 :class:`Response` on admission, or a canonical error\n    :class:`Response` (404 reserved prefix, 421 no destination, 413\n    oversized, or any admission refusal).",
        "operationId": "phantom_path_put",
        "parameters": [
          {
            "in": "path",
            "name": "phantom_path",
            "required": true,
            "schema": {
              "title": "Phantom Path",
              "type": "string"
            }
          }
        ],
        "responses": {
          "200": {
            "content": {
              "application/json": {
                "schema": {}
              }
            },
            "description": "Successful Response"
          },
          "422": {
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/HTTPValidationError"
                }
              }
            },
            "description": "Validation Error"
          }
        },
        "summary": "Raw Intake"
      }
    }
  }
}
