# Regression coverage matrix (plan § 6.2 / peer-review § C-2)

Every Critical + High audit ID must have AT LEAST ONE regression
test in this directory or a documented path to an equivalent
regression elsewhere in the suite. The Phase 8 § 9.2.7 phantom-
revert sweep reads this matrix as the authoritative coverage
inventory.

| ID | Severity | Closure | Regression test path |
|---|---|---|---|
| C1 | Critical | Orphan body GC (Phase 2 § 3.2.1) | `src/phantom-service/tests/unit/test_admin_bulk_delete_c1.py` (Phase 2 unit). E2E body-lost-then-cleaned coverage falls out of the body-orphan janitor unit tests in `src/phantom-service/tests/unit/test_body_orphan_janitor.py`. |
| C2 | Critical | Saturation try/finally (Phase 2 § 3.2.2 / H1) | Release-on-failure tests in `src/phantom-service/tests/unit/test_admission.py` (saturation_refused, idempotency collision, codec failure, body-store failure, namespace-clear failure) + `test_auth_kicker_noop_releases_admitted_slot.py` + `test_auth_kicker_releases_on_store_exception.py` (same directory). |
| H1 | High | Saturation try/finally (Phase 2 § 3.2.2) | Same as C2: H1 and C2 collapse into one fix; the C2 row's tests are the regression. |
| H2 | High | Content-Length precheck (Phase 2 § 3.2.3) | Content-length precheck tests in `src/phantom-service/tests/unit/test_send_route.py` (Phase 2). |
| H3 | High | TOCTOU race; closed structurally + H9 regression | `tests/e2e/regression/test_h9_h3_toctou.py` (H9 is the regression for H3 closure; see plan § 0.2 line 88). |
| H4 | High | Body-retention contract + recovery carve-out (Phase 2 § 3.2.4) | `src/phantom-service/tests/unit/test_h4_body_retention_contract.py` + `src/phantom-service/tests/unit/test_recovery.py::test_recovery_h4_carveout_discarded_body_not_quarantined` (Phase 2). |
| H6 | High | AdMinter into composition-root TaskGroup (Phase 2 § 3.2.5) | `src/phantom-service/tests/unit/test_h6_ad_minter_supervised.py` (Phase 2). The H10(b) test below covers the cancellation contract end-to-end. |
| H7 | High | Atomic admission (Phase 1 § 2.3.17) | Phase 1 Slice 1.F unit tests + `tests/e2e/crash_recovery/test_crash_admission_atomic.py` (Phase 5) + `scripts/check_atomic_admission.py` (static check, Phase 5 § 6.2.8). |
| H8 | High | BodyMissingError → corrupted (Phase 2 § 3.2.6) | `src/phantom-service/tests/unit/test_h8_body_missing_corrupted.py` (Phase 2) + `tests/e2e/test_files_lost_midway.py::test_manual_body_file_lost_post_persist` (Phase 5) + `tests/e2e/regression/test_h10_silent_route_and_supervision.py::test_h10a_missing_body_raises_body_missing_error` (Phase 5). F2 (review 08-12) widened the closure from whole-body loss to PARTIAL loss, which the body store cannot detect on its own: `tests/e2e/test_multipart_corrupted.py::test_multipart_single_missing_ref_transitions_to_corrupted` (one ref of three deleted) + `src/phantom-service/tests/unit/test_partial_body_refs_corrupted.py` (partial and empty-directory shapes, slot release, both comparison directions). |
| H9 | High | TOCTOU regression for H3 (Phase 5 § 6.2.9) | `tests/e2e/regression/test_h9_h3_toctou.py` (two tests: cancel-during-attempt + replay-during-attempt). |
| H10 | High | Runtime silent-route + production-process TaskGroup supervision and recovery | `tests/e2e/regression/test_sender_unknown_fault_supervision.py` injects deterministic unknown faults before and after durable claim through the real CLI/composition root, requires the pinned uvicorn SIGTERM status plus fatal-bridge record, inspects stopped SQLite state, and proves an unpatched restart reconstructs saturation `1 → 0` while emitting exactly one append-only metadata-create/PUT event sequence. `tests/e2e/regression/test_h10_silent_route_and_supervision.py` retains only the lower-layer body-missing and generic TaskGroup semantics checks. |
| ADR-011 | High | Capture-observation expiry reexecution is opt-in; operators must verify both identity/idempotency and upstream capability semantics/lifetime. Keyed replay preserves upstream identity but does not necessarily renew an exact cached capability, while unkeyed replay exposes duplicate creation. | `tests/e2e/test_e2e_06_capture_expiry.py` strictly proves the enabled-keyed, enabled-unkeyed, and omitted-default-disabled matrix through a real two-step chain. The upload-scoped `error_rate_5xx` oracle proves at least one PUT rejected by that branch before rewind. The append-only successful-event oracle separately pins two cached-identity create responses plus one accepted PUT, two distinct-identity create responses plus one accepted PUT, and one create response plus zero accepted PUTs, respectively. |
| H13/D3 | High | architecture-intent supervision text (Phase 2 § 3.2.7) | Documentation update: no test surface; covered by the falsifiability-descriptions CI job which lints docstring/description drift. |
| D1 | Med-High | Re-submitting a live `chain_id` under a fresh idempotency key returns a structured 409 `chain_id_in_use` (ADR-017), never a naked 500 (Defender R2: typed `InsertClaimOutcome`, `CHAIN_ID_COLLISION` arm). | `tests/e2e/regression/test_d1_duplicate_chain_id.py`. |
| E1 | Low | Duplicate multipart `body_refs[<name>]` parts are rejected with a structured 422 `body_ref_duplicate` instead of a silent last-wins overwrite (Defender R2 parser guard). | `tests/e2e/regression/test_e1_duplicate_multipart_body_ref.py`; parser-level unit coverage in `src/phantom-service/tests/unit/test_parser.py`. |
| G1 | Low-Med | Reusing an `X-Phantom-Idempotency-Key` with a DIFFERENT body is rejected 422 `idempotency_key_conflict` via the body-hash divergence check; an identical body still replays at 200 (Defender R2). | `tests/e2e/regression/test_g1_idempotency_key_reuse_different_body.py`. |
| V1 | High | Recovery cursor-drain over a SIGKILL-hot WAL (Defender R6): `run_recovery` collects quarantine targets, drains the `iter_rows` cursor, THEN writes; store also checkpoint-truncates a hot WAL at `start()`. | `tests/e2e/crash_recovery/test_crash_sigkill_recovery_no_database_locked.py` (real `os.kill(SIGKILL)`, `[hybrid]` param = RAM-lost quarantine path). |
| V2 | High | Same fix as V1: the recovery lock hit every mode; the all_disk file-missing quarantine path goes through the same `mark_corrupted` write. | Same file, `[all_disk]` param (file-missing quarantine path). |
| V5 | Low-Med | Retry-cadence pins (aggressor Round 5, adopted exploratory): the retry budget reaches a terminal state, the backoff interval is held (no hammering at the poll rate), and the retry schedule (`next_attempt_at` + attempts) survives SIGKILL. The V5-C jitter fix is asserted at unit level in `src/phantom-service/tests/unit/test_strategies.py`. | `tests/e2e/regression/test_v5_retry_cadence_and_crash_survival.py` (subprocess harness). |
| R7-4b | High | A duplicate submit of an in-flight chain_id no longer destroys the original (Defender R8): admission pre-checks the chain_id PK BEFORE `body_store.put`, so a duplicate of a live chain_id is rejected 409 without touching the original's body; the CHAIN_ID_COLLISION arm no longer deletes the shared body. | `tests/e2e/regression/test_r74b_duplicate_chainid_preserves_inflight.py` (original survives + delivers `succeeded`). |
| R7-5-A | Holds (no defect) | Ambiguous outcome: the upstream stored the body but the ack back to Phantom was reset/truncated. Delivery stays exactly-once via at-least-once retry + dedup; no double-delivery on the terminal upload step. Adopted exploratory pin (Defender R8); the property held on current code. | `tests/e2e/regression/test_r75a_ambiguous_outcome_exactly_once.py`. |
| R7-5-B | Medium | A 2xx response missing a required capture no longer wedges the chain + leaks a slot (Defender R8): the executor validates declared-and-downstream-referenced captures before advancing; a missing one is a retryable `CaptureIncomplete`. | `tests/e2e/regression/test_r75_2xx_missing_capture_no_wedge.py` (chain reaches a clean terminal). |
| R7-1-D / R7-2-B | Medium-High | A SQLITE_IOERR / SQLITE_FULL commit failure no longer leaks an open transaction / wedges the shared connection (Defender R8): `_write_txn` rolls back on any failure across every writer + the token cache. | `src/phantom-service/tests/unit/test_r71_r72_storage_fault_failclosed.py` (open-txn parametrized: ioerr + full). |
| R7-1-A/B / R7-2-A | Low-Med | A storage-layer OSError (fsync EIO / ENOSPC) during admission body buffering no longer escapes as a naked 500 (Defender R8): mapped to the new ADR-017 `storage_unavailable` 503. | `src/phantom-service/tests/unit/test_admission.py::test_admit_chain_releases_slot_on_body_store_failure` (typed code + slot released) + the `storage_unavailable` contract tests. |
| R9-V6-2 | High | An external DB lock held past `busy_timeout` during recovery boot no longer crashes startup / wedges the service (Defender R9): `run_recovery` rides out a transient lock via a bounded retry-with-backoff on `is_transient_lock_error` (recovery is idempotent; R7-3); a persistent lock past the budget surfaces a clean `RecoveryLockError`, never a raw traceback. | `tests/e2e/crash_recovery/test_r9_v6_recovery_boot_survives_external_lock.py` (seeded data_dir + external lock held 45 s during a fresh boot → service still becomes healthy + all seeded rows survive). |
| N1 | High | Malformed inline base64 is rejected at admission (422 `envelope_invalid`) instead of crash-looping the sender (review 08-12, found during plan review). `ChainBodyBytes.value_b64` was unvalidated and the executor decoded it bare, so a producer body of `{"kind": "bytes", "value_b64": "A"}` was admitted 202 and then killed the sender's TaskGroup on every claim. Both decoder failure classes are covered: malformed base64 and non-ASCII input, which raise different exception types. The executor also classifies already-admitted rows as terminal `failed` rather than dying. | `tests/e2e/regression/test_n1_inline_body_base64_rejected.py` (parametrised over both classes: 422 at ingress, no chain id, and a healthy chain still delivers afterwards) + `src/phantom-service/tests/unit/test_inline_body_base64_validation.py` (parse rejection, newline-wrapped counter-test, executor classification, sender termination with slot release, and the single-decode-definition text scan). |
| F1 | High | A step whose host matches no configured route no longer escapes the sender as a bare `ValueError` (review 08-12 Tier 1): the executor classifies the miss as `RouteUnresolved` and the sender parks the row in terminal `stored` (body and slot retained, never re-claimed, replay-eligible), so one unroutable row can no longer crash-loop the process and strand the whole backlog. Distinct from the H10 row, which pins the supervision bridge for genuinely unknown faults; F1 narrows what counts as unknown without weakening it. | `tests/e2e/test_e2e_unroutable_step_parks.py` (poison row parks in `stored`, a later routable chain still reaches `succeeded` and delivers) + `src/phantom-service/tests/unit/test_route_unresolved_parks.py` (classification, token sanitisation, slot retention, detection counter). |
| F4 | High | Raw intake no longer drops the inbound query string (review 08-12 Tier 1). Both destination carriers now forward every inbound parameter byte-for-byte except Phantom's own reserved `phantom` carrier, so a multipart part upload reaches the upstream as a part upload rather than overwriting the whole object with one part's bytes, and a presigned credential (which lives in the query) survives. On an `aws_sigv4` route the client's presigned parameter set is additionally stripped before signing, because Phantom's signature supersedes it (ADR-033) and forwarding both presents two authentication mechanisms. | `tests/e2e/test_e2e_raw_intake_query_preserved.py` (query-addressed raw PUT delivers and the sink records the exact query) + `src/phantom-service/tests/unit/test_catch_all_route.py` (the eight carrier tests: both carriers, the percent-encoded carrier key, the fragment rule, byte-for-byte preservation, and the two no-trailing-`?` counter-tests) + `src/phantom-service/tests/unit/test_sigv4_presigned_supersede.py` (the precedence rule, the `none`/`phantom_bearer` counter-test, and the closed-set check). |
| F9 | High | Hop-by-hop and framing headers are no longer persisted onto the synthesized step or forwarded upstream (review 08-12 Tier 1). A raw upload that arrived chunked left `Transfer-Encoding: chunked` in the persisted envelope, and httpx only `setdefault`s its own framing, so h11 emitted BOTH framing headers over a fixed-length body on every attempt; the upstream rejected each one and the row was permanently undeliverable. The set now has ONE definition in `chain/executor.py` with two enforcement points, and the executor's strip is the guarantee, which is the only guard the ENVELOPE path has. `Content-Encoding: aws-chunked` and `x-amz-decoded-content-length` are deliberately kept, because they describe the body bytes. | `tests/e2e/test_e2e_raw_intake_chunked_framing.py` (a genuinely chunked raw PUT delivers and no framing header reaches the sink) + `src/phantom-service/tests/unit/test_hop_by_hop_strip.py` (the four executor-level tests: the full set, the `Connection`-token expansion, the aws-chunked carve-out, and the one-framing-mechanism guarantee) + `src/phantom-service/tests/unit/test_catch_all_route.py::test_catch_all_does_not_persist_framing_headers`. |
| F7 | High | The SigV4 re-sign no longer emits duplicate headers (review 08-12 Tier 1). botocore's header map is case-insensitive and re-adds every header it rewrites canonical-cased, while the caller's dict is case-sensitive, so a key-by-key copy-back left the client's lowercase originals beside Phantom's fresh values. A raw-intake request that the client had already signed therefore reached the wire with two `Authorization`, two `x-amz-date` and two `x-amz-content-sha256` lines; S3 answers 403 SignatureDoesNotMatch, the executor classifies that as `FailedAuth`, marks the host credential bad, and every row for that destination parks. The map is now REBUILT from the signed view, so a duplicate is structurally impossible for every header botocore rewrites rather than for the three known today. | `tests/e2e/test_e2e_sigv4_resign_round_trip.py::test_sigv4_resign_replaces_a_client_supplied_authorization` (a client-signed raw PUT still delivers and the sink records exactly one Authorization, Phantom's) + `src/phantom-service/tests/unit/test_sigv4_signer_no_duplicate_headers.py` (the starlette-shaped seed, the unsigned-header counter-test, the botocore-uniqueness precondition, and the in-place mutation contract). |
| F11 | High | Ingress no longer writes a client-supplied `Authorization` into the bearer token cache for a non-bearer route (review 08-12 Tier 1, via decision D3). A raw intake on an `aws_sigv4` route pushed a per-request SigV4 credential string into the `(host, uid)` bearer slot, which overwrote a real operator-pushed bearer, flipped a `bad` slot back to `fresh` in the admin token view, and fired the token cache's wake handlers on every raw PUT so both kickers rescanned. Admission now consults the resolved route's `auth_mode` and caches only for `phantom_bearer`; a bearer route still caches, which is the documented pilot behaviour. | `tests/e2e/regression/test_f11_sigv4_intake_does_not_poison_bearer_cache.py` (a raw SigV4 intake is still admitted 202 and the admin token surface stays empty) + `src/phantom-service/tests/unit/test_ingress_cache_auth_mode_gate.py` (all four cases: the three `AuthMode` members and the no-route miss, plus the bad-slot revival and the wake-handler arms). |
| CL8 | Cleanup (High-impact) | The raw-intake ack now carries the canonical SIX `X-Phantom-*` headers from the shared builder instead of two hand-built ones (review 08-12). The SDK's `ResponseHeaders` is `strict=True, extra="forbid"` and requires five of them, so `parse_response_headers` raised `PhantomEnvelopeError` on a SUCCESSFUL raw upload and any SDK-based tooling pointed at the raw-intake surface failed. The change is purely additive: no existing header changes name, value or presence, so a stock S3 client that ignores unknown response headers is unaffected. | `tests/contract/test_raw_intake_ack_parses_with_client_model.py` (the two surfaces agree) + `tests/e2e/test_e2e_raw_intake_query_preserved.py::test_raw_intake_ack_is_sdk_parseable` (the live 202 over the real wire) + `src/phantom-service/tests/unit/test_catch_all_route.py` (the six-header, group-id-equals-chain-id and purely-additive tests). |
| N3 | High | A raw-intake object key containing a `{{...}}` span no longer destroys the upload (review 08-12, ruled in during Phase 2). Object keys are arbitrary bytes and the catch-all deliberately puts the bucket and key only into the step URL, which the executor treated as a template: substitution failed, and the row terminated `failed` with a template error it never had a template for, body retained but undeliverable. `ChainEnvelope` now carries a `templated` marker (default `true`, so authored chains are unchanged) and the raw-intake adapter sets it false, which suppresses all six interpretation points: the parser's static placeholder pass, the capture-TTL gate, and the four substitution sites. Escaping the key instead was rejected because it would rewrite the request line Phantom forwards. | `tests/e2e/regression/test_n3_raw_intake_braced_key.py` (a braced-key raw PUT reaches `succeeded` and the sink holds the object under the braced key) + `src/phantom-service/tests/unit/test_literal_chain_no_substitution.py` (the adapter witness, the parser gate with its counter-test, the capture-TTL gate, headers and body, the templated-chain counter-test, and the no-migration default). |
| R9-V6-1 | Medium | An external DB lock held past `busy_timeout` during admission no longer surfaces as a naked 5xx / `PhantomTimeoutError` (Defender R9): admission maps the transient-lock `OperationalError` to the ADR-017 `storage_unavailable` 503, and `SQLITE_BUSY_TIMEOUT_MS` lowered 5000→1000 so a contended writer fails fast (a clean retryable) instead of monopolizing the single `_write_lock` slot and timing out the client. | `tests/e2e/regression/test_r9_v6_external_lock_admission_clean_503.py` (8-deep admission burst under a held external lock → every response is a success or a clean 503; service recovers after release). |

## How Phase 8 deliberate-violation tests use this matrix

Phase 8 § 9.2.4 will revert each fix and assert the regression
test trips. The matrix tells the Phase 8 author which test file
to expect a green→red transition in. If a fix is reverted and
the listed test still passes, the regression test is insufficient
and must be hardened.

## Adding new entries

When a new Critical or High audit ID lands, add a row above with:

- The ID + severity classification.
- The closure phase + task ID.
- The path to the regression test (must exist; "to be added" is
  not acceptable; author the test first).
